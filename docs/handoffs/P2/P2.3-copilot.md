# Handoff P2.3 — GitHub Copilot/GPT-5.4

## Status

Complete

## Inputs read

- `AGENTS.md`
- `KE_HOACH_XUYENLAB_CHEMISTRY.md`
- `docs/adr/0002-content-lifecycle.md`
- `docs/adr/0004-agent-ownership-and-handoffs.md`
- `docs/contracts/content.md`
- `docs/contracts/handoff.md`
- `docs/source-manifest.csv`
- `scripts/import-docx/README.md`
- `docs/handoffs/P1/SUMMARY.md`
- `node_modules/next/dist/docs/01-app/02-guides/testing/vitest.md`

## Completed

- Added `scripts/import-docx/failure-report.schema.json` as the machine-readable Phase 2 failure-report contract.
- Corrected the fallback contract defect found in integration review: asset-backed fallback metadata is now strategy-conditional rather than required for every `fallback` outcome.
- Added a bounded synthetic regression corpus under `tests/fixtures/converter/` covering:
  - successful semantic heading/paragraph/list/formula/table blocks;
  - unsupported drawing, SmartArt, chart and embedded-object cases;
  - warning vs blocking severity;
  - asset-backed fallback metadata for `chemfigure` and `rasterized-inline`;
  - visible warning placeholders for `manual-review` without fake asset metadata;
  - `datatable-html` fallback without image-only metadata such as `altText`;
  - explicit `strategy: "none"` for omitted objects;
  - aggregate summary counts;
  - rerun/idempotency expectations with stable block IDs, source locators and source digest.
- Added Vitest coverage under `tests/converter/` for:
  - valid report acceptance against the JSON Schema;
  - malformed report rejection;
  - manifest/source-path coordination with the frozen content contract;
  - full block accounting from synthetic input to report output;
  - stable rerun behavior apart from run metadata.
- Expanded `vitest.config.mts` so repository tests include `tests/**/*.test.{ts,tsx}`.
- Kept all work inside the assigned file boundary and did not modify converter implementation, source DOCX files or source HTML files.

## Decisions and assumptions

- Used JSON Schema draft-07 so the schema can be validated with the repo's current installed toolchain and no dependency-manifest changes.
- Modeled the regression corpus as normalized synthetic JSON inputs plus expected report JSON outputs because the Phase 2 converter entrypoint is not present yet.
- Bound fixtures to manifest-backed sources `T06-S01` and `T08-S01` so `sourceId` and `sourcePath` stay aligned with `docs/source-manifest.csv` and the frozen content contract.
- Treated idempotency for this task as stable report content for the same normalized input: same block IDs, locators, counts and `sourceDigest`, with only run metadata allowed to differ.
- Constrained fallback metadata by strategy:
  - `chemfigure` and `rasterized-inline` must carry real `assetPath` and `altText`;
  - `manual-review` must not require or emit fake asset metadata and may use a visible placeholder caption instead;
  - `datatable-html` does not require `altText` because it represents HTML table content rather than an image fallback.

## Files and migrations changed

- `scripts/import-docx/failure-report.schema.json` — failure-report JSON Schema for Phase 2 converter output.
- `tests/converter/report-contract.ts` — fixture loader, manifest parser and contract-level validation helpers.
- `tests/converter/report-contract.test.ts` — corpus, manifest, summary and rerun/idempotency tests.
- `tests/converter/report-schema.test.ts` — schema acceptance/rejection tests.
- `tests/fixtures/converter/corpus/topic-06-hybrid-sample/input.json` — synthetic mixed semantic/fallback/omitted fixture input.
- `tests/fixtures/converter/corpus/topic-06-hybrid-sample/report.json` — expected valid hybrid report for Topic 6 fixture.
- `tests/fixtures/converter/corpus/topic-06-placeholder-sample/input.json` — synthetic manual-review and `datatable-html` fallback input.
- `tests/fixtures/converter/corpus/topic-06-placeholder-sample/report.json` — expected valid placeholder/table-fallback report without fake image metadata.
- `tests/fixtures/converter/corpus/topic-08-rerun-sample/input.json` — synthetic rerun/idempotency fixture input.
- `tests/fixtures/converter/corpus/topic-08-rerun-sample/report.json` — expected first-run report for Topic 8 fixture.
- `tests/fixtures/converter/corpus/topic-08-rerun-sample/rerun.report.json` — expected rerun report for Topic 8 fixture.
- `tests/fixtures/converter/invalid/schema/*.report.json` — malformed reports that must fail schema validation.
- `tests/fixtures/converter/invalid/contract/*.report.json` — reports that pass shape validation but fail stable-ID/count/manifest contract checks.
- `vitest.config.mts` — added `tests/**/*.test.{ts,tsx}` to Vitest include patterns.
- Migrations: none.

## Verification

- `npm ci` — exit 0.
- `npx prettier --check vitest.config.mts tests/converter tests/fixtures/converter scripts/import-docx/failure-report.schema.json` — exit 1 before formatting; then fixed with `npx prettier --write ...` exit 0; rerun `npx prettier --check ...` exit 0.
- `npx vitest run tests/converter/report-schema.test.ts tests/converter/report-contract.test.ts` — exit 0.
- `npm run verify` — exit 0.
- Correction run on 2026-08-11:
  - `npx prettier --check vitest.config.mts tests/converter tests/fixtures/converter scripts/import-docx/failure-report.schema.json docs/handoffs/P2/P2.3-copilot.md` — exit 0 after the schema correction.
  - `npx vitest run tests/converter/report-schema.test.ts tests/converter/report-contract.test.ts` — exit 0 after adding strategy-conditional fallback cases.
  - `npm run verify` — exit 0 after the schema correction.
- Omitted: `npm run test:e2e`; reason: this task only changed converter-schema/corpus contracts and the repository's required aggregate validation for completion is `npm run verify`.

## Environment and external services

- Local toolchain only.
- Node.js `v24.19.0`
- npm `11.17.0`
- No project environment variables or external service credentials were required.

## Remaining work, known issues and risks

- The converter itself is still not wired to this corpus. P2.1 or a later integration task must connect real prototype output to the schema and fixtures.
- The schema tests currently validate through the installed lockfile toolchain without adding new declared dependencies. If the lockfile stops exposing a JSON Schema validator transitively, a future bounded task should add or replace that validator intentionally.
- The synthetic corpus proves the contract, not Word/HTML fidelity. Real extracted pilot outputs from Topic 6/8 still need black-box integration coverage once P2.1 exposes a runnable converter surface.
- If the converter later needs persisted non-image fallback artefacts for `datatable-html` or `manual-review`, that should be expressed with a distinct contract field rather than overloading `assetPath`.

## Plan deviations

- None.

## Required inputs for the next task/phase

- P2.1 must expose a deterministic black-box converter surface that can be invoked from Node/Vitest and returns a JSON report conforming to `scripts/import-docx/failure-report.schema.json`.
- That surface must provide stable per-block identifiers and locators equivalent to the synthetic fixture shape:
  - `id` in the form `Txx-Syy:<kind><nnnn>`;
  - `sourceLocator.sectionPath`;
  - `sourceLocator.blockOrder`;
  - `sourceLocator.pathHint`;
  - optional `sourceLocator.pageHint` and `sourceLocator.textAnchor`.
- The real converter output for repeated runs of the same normalized source must preserve `sourceDigest`, block ordering, block IDs, block locators and summary counts, changing only run metadata.
- Once P2.1 is available, add a bounded black-box integration test layer that feeds extracted Topic 6/8 pilot data into the converter and compares the emitted report to these contract expectations.
