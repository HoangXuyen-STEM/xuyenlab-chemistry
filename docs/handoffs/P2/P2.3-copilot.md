# Handoff P2.3 — GitHub Copilot/GPT-5.4

## Status

Complete

## Inputs read

- `AGENTS.md`
- `KE_HOACH_XUYENLAB_CHEMISTRY.md`
- `docs/adr/0002-content-lifecycle.md`
- `docs/adr/0004-agent-ownership-and-handoffs.md`
- `docs/contracts/content.md`
- `docs/contracts/handoff.md`
- `docs/source-manifest.csv`
- `scripts/import-docx/README.md`
- `docs/handoffs/P1/SUMMARY.md`
- `node_modules/next/dist/docs/01-app/02-guides/testing/vitest.md`
- P2.1 prototype and its handoff during integration.

## Completed

- Added `scripts/import-docx/failure-report.schema.json` as the machine-readable
  Phase 2 failure-report contract.
- Corrected the fallback contract defect found in integration review: asset-backed
  fallback metadata is strategy-conditional rather than required for every
  `fallback` outcome.
- Added a bounded synthetic regression corpus under `tests/fixtures/converter/`
  covering semantic and fallback blocks, malformed reports, manifest provenance,
  aggregate counts, and rerun/idempotency expectations.
- Added Vitest coverage for schema acceptance/rejection, manifest/source-path
  coordination, full synthetic block accounting, and stable reruns.
- Expanded `vitest.config.mts` so repository tests include
  `tests/**/*.test.{ts,tsx}`.
- Added `tests/converter/prototype-black-box.test.ts`, which invokes the real
  Python converter against manifest-backed `T06-S02` HTML in a unique temporary
  directory. It validates the emitted report against the JSON Schema, confirms
  manifest provenance, confirms all 26 HTML list blocks are represented,
  confirms `H<sub>2</sub>O<sub>2</sub>` is retained in generated MDX, and
  confirms the second run is unchanged without overwriting output.
- Declared `ajv` directly in `devDependencies`; it is no longer relied on as an
  accidental transitive dependency.

## Decisions and assumptions

- Used JSON Schema draft-07 for compatibility with Ajv and the existing test
  toolchain.
- Kept synthetic corpus tests because they cover invalid shapes and fallback
  strategy combinations that a single real source does not contain.
- Bound real black-box coverage to `T06-S02`, whose manifest-backed HTML source
  has 26 list items and chemistry subscript notation needed to regress the
  defects found in P2.1 review.
- The black-box test creates and removes only its own `mkdtemp` directory under
  the operating-system temporary directory.

## Files and migrations changed

- `scripts/import-docx/failure-report.schema.json` — failure-report JSON Schema.
- `tests/converter/report-contract.ts` and its tests — synthetic corpus contract
  validation, manifest parsing and rerun checks.
- `tests/converter/report-schema.test.ts` — schema acceptance/rejection tests.
- `tests/fixtures/converter/**` — valid and invalid synthetic regression corpus.
- `tests/converter/prototype-black-box.test.ts` — real converter integration test.
- `package.json`, `package-lock.json` — direct Ajv development dependency.
- `vitest.config.mts` — test include pattern.
- `docs/handoffs/P2/P2.3-copilot.md` — this handoff.
- Migrations: none.

## Verification

- `npx vitest run tests/converter/report-schema.test.ts
tests/converter/report-contract.test.ts` — pass (7 tests).
- `npx vitest run tests/converter/prototype-black-box.test.ts` — pass (1 test).
- `npm run verify` — pending the integration-owner final run after the black-box
  update; earlier task-level runs passed.
- `npm run test:e2e` was omitted because P2.3 changes converter contracts and
  Node-side integration tests; renderer/browser verification belongs to P2.2 and
  the integration review.

## Environment and external services

- Local Node.js, Python 3 and repository sources only.
- No project environment variables, external services or credentials required.

## Remaining work, known issues and risks

- Real black-box coverage currently covers only the `T06-S02` HTML pilot. The
  DOCX path and Topic 8 remain future regression candidates.
- The synthetic corpus proves contract edge cases; it does not prove Word/HTML
  chemical fidelity.
- If future converter behavior needs persisted artefacts for `datatable-html` or
  `manual-review`, use a new contract field rather than overloading `assetPath`.

## Plan deviations

- None. The direct Ajv declaration is an integration hardening change that removes
  the transitive-dependency risk documented in the original P2.3 handoff.

## Required inputs for the next task/phase

- Project owner comparison of 10–20 representative Topic 6/8 sections using the
  P2.1 checklist.
- Explicit owner A/B/C decision before P2 closes or P3 begins.
- Future converter work should extend black-box coverage to DOCX and Topic 8
  without changing the frozen report contract silently.
