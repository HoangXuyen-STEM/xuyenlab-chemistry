# Handoff P2.4 — Codex

## Status

Complete — staging review fixture is ready and the project owner selected strategy B
(hybrid) on 2026-08-11.

## Inputs read

- `AGENTS.md`
- `KE_HOACH_XUYENLAB_CHEMISTRY.md` (P2 scope and exit expectations)
- `docs/adr/0001-platform-architecture.md`
- `docs/adr/0002-content-model-and-versioning.md`
- `docs/adr/0004-agent-ownership-and-handoffs.md`
- `docs/contracts/content-and-import.md`
- `docs/contracts/handoff.md`
- `docs/handoffs/P1/SUMMARY.md`
- `docs/handoffs/P2/P2.1-codex.md`
- `docs/handoffs/P2/P2.2-claude.md`
- `docs/handoffs/P2/P2.3-copilot.md`

## Completed

- Added the staging-only route `/fixtures/conversion-review` with 12 curated excerpts from current converter output: three each from T06-S01, T06-S02, T08-S01 and T08-S02.
- The page identifies each source ID, exact source path and source role; it gives the project owner a three-step comparison process and the A/B/C decision required to close P2.
- Added explicit non-publication labeling and `noindex`; no canonical lesson, database record or lesson status was created or changed.
- Fixed an integration defect exposed by the fixture: the converter now places valid HTML table children inside `DataTable`, rather than unsupported Markdown table syntax. The T06-S02 black-box regression asserts this boundary.
- Regenerated the four temporary converter runs under `/tmp/xuyenlab-p24-review` to select the review excerpts. Reported counts were: T06-S01 281 blocks / 96 blocking fallbacks / 3 warning fallbacks; T06-S02 109 / 3 / 4; T08-S01 708 / 126 / 42; T08-S02 243 / 0 / 18.

## Decisions and assumptions

- This route is a review aid, not an importer output location. The excerpts are intentionally narrow, visibly labeled staging-only, and preserve known fallback identifiers where shown.
- The project owner selected B — hybrid: text/headings/tables may be converted
  semantically; complex formulas, objects and figures require reviewed image fallbacks
  with alt text/captions. The later source selection remains subject to provenance
  review; HTML remains derivative and never authorizes automatic publication.
- Extracted image assets are not deployed to the preview. Figure comparison is therefore **UNVERIFIED** and the route says so explicitly; it must be done against the local DOCX/HTML sources.
- The table markup correction is a renderer compatibility fix within P2.1’s converter scope; it does not change the content/import contract or fallback classifications.

## Files and migrations changed

- `scripts/import-docx/prototype.py` — emits renderer-valid HTML tables inside `DataTable`.
- `tests/converter/prototype-black-box.test.ts` — guards the HTML table output boundary.
- `content/fixtures/conversion-review/*.mdx` — 12 staging-only converter-output excerpts.
- `src/app/fixtures/conversion-review/*` — review route, metadata, styles and route test.
- `docs/handoffs/P2/P2.4-codex.md` — this handoff.
- No migrations.

## Verification

- `npm test -- --run tests/converter/prototype-black-box.test.ts src/app/fixtures/conversion-review/page.test.tsx` — passed: 2 files, 3 tests.
- `npm run verify` — format, lint, typecheck and all tests passed (14 files, 56 tests); build was **UNVERIFIED**: after clearing a stale `.next/lock`, local Turbopack stopped after “Creating an optimized production build” without a diagnostic, and the Webpack fallback reported an incomplete TypeScript `--showConfig` parse even though `./node_modules/.bin/tsc --showConfig` succeeded independently. Vercel/CI preview is required as an independent build verification.
- `git diff --check` — passed.

## Environment and external services

- No environment variables or external services added or changed.
- Vercel preview deployment is required after the integration branch is pushed.

## Remaining work, known issues and risks

- P1 / project owner: the A/B/C decision is recorded as B. Per-sample comparison
  evidence is **UNVERIFIED** because it was not supplied to the integration owner.
- P0 / project owner: figures are **UNVERIFIED** in the preview because draft extracted assets are intentionally not deployed.
- P1 / Codex integration owner: after the owner decision, write the P2 phase summary and determine the phase-exit verdict.
- P1 / later content work: all mathematical/embedded-object fallbacks remain draft-only and require chemistry/editorial review; they cannot be published automatically.

## Plan deviations

- None. The table output correction fixes converter-to-renderer compatibility discovered while making the approved P2.4 review fixture; no ADR or contract was changed.

## Required inputs for the next task/phase

- This handoff and the four P2 task handoffs.
- The P2 integration preview at `/fixtures/conversion-review`.
- The four source files named on the fixture cards.
- The recorded project-owner decision B and the resulting P2 summary.
