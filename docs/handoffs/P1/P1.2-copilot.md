# Handoff P1.2 — GitHub Copilot

## Status

Complete

## Inputs read

- `KE_HOACH_XUYENLAB_CHEMISTRY.md`
- `AGENTS.md`
- `docs/adr/0001-platform-architecture.md`
- `docs/adr/0004-agent-ownership-and-handoffs.md`
- `docs/contracts/backend.md`
- `docs/contracts/storage-and-environment.md`
- `docs/contracts/handoff.md`
- `docs/handoffs/P0/SUMMARY.md`
- `docs/handoffs/P1/P1.1-codex.md`
- `package.json`, `package-lock.json` and `.nvmrc`
- Version-matched Next.js CI build cache guidance under `node_modules/next/dist/docs/`

## Completed

- Added a GitHub Actions `CI` workflow for pull requests and pushes to `main`.
- Pinned the workflow runtime to the repository's Node 22 `.nvmrc` target.
- Added npm dependency caching and a lockfile/source-keyed Next.js build cache.
- Runs `npm ci`, format check, lint, typecheck, unit tests and the production build.
- Kept Playwright E2E out of the fast foundation workflow; it requires a browser
  install and remains available as a separate local command.

## Decisions and assumptions

- The stable job name is `Validate`; integration owner may add this check to branch
  protection after its first successful GitHub Actions run.
- The CI workflow follows ADR-0001's GitHub Actions validation decision and P1.1's
  npm/Node 22 foundation decision.
- No application architecture, database schema, package dependency or production
  workflow changed.

## Files and migrations changed

- `.github/workflows/ci.yml`: Node 22 validation workflow and caches.
- `docs/handoffs/P1/P1.2-copilot.md`: this task handoff.
- No database migration was added or run.

## Verification

- `npx prettier --check .github/workflows/ci.yml` — exit 0.
- `npm run verify` — exit 0: format, lint, typecheck, 2 unit-test files / 4 tests,
  and production build passed.
- The workflow has not yet run on a GitHub-hosted Node 22 runner; verify its first
  pull request or `main` run before making it a required branch-protection check.

## Environment and external services

- GitHub Actions uses `actions/checkout@v4`, `actions/setup-node@v4` and
  `actions/cache@v4`.
- No secrets, cloud service credentials or production environment are used.

## Remaining work, known issues and risks

- Integration owner: review the first GitHub Actions run, then protect `main` with
  the `Validate` check if it is green.
- P1.3: complete documentation-only UX specification work.
- P1 integration owner: write `docs/handoffs/P1/SUMMARY.md` after P1.2/P1.3 and
  phase exit checks are complete.

## Plan deviations

None.

## Required inputs for the next task/phase

- First successful `CI / Validate` GitHub Actions run on Node 22.
- P1.3 handoff and UX specification.
- Integration-owner P1 summary and phase-exit review before P2 starts.
