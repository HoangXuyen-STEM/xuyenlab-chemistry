# Handoff P0.3 — Codex GPT-5.6 Terra

## Status

Complete

Local Git, collaboration scaffolding, first publication and owner-side `main` protection are complete.

## Inputs read

- `KE_HOACH_XUYENLAB_CHEMISTRY.md`
- `AGENTS.md`
- `docs/adr/0004-agent-ownership-and-handoffs.md`
- `docs/contracts/handoff.md`
- `docs/handoffs/P0/SUMMARY.md`
- Owner-provided GitHub repository URL

## Completed

- Initialized a valid Git repository on branch `main`.
- Configured `origin` as the private GitHub repository.
- Added ignore and binary-file attributes.
- Added secret/student-data policy.
- Added common agent instructions plus Claude Code and Copilot-specific entry points.
- Added phase-task, bug-report and pull-request templates.
- Documented task lifecycle, branch names, shared-file locking, labels and owner-side branch protection.
- Created the root commit containing source documents and P0 durable documentation.

## Decisions and assumptions

- Track the initial 127.47 MiB Word source set as normal Git binary files because Git LFS is unavailable and the largest file is about 17 MiB.
- Reassess Git LFS before repeated bulk source-version updates.
- Remote rules/labels remain owner-controlled. The first push was performed only after explicit approval.

## Files and migrations changed

- `.gitignore`, `.gitattributes`
- `README.md`, `SECURITY.md`, `AGENTS.md`, `CLAUDE.md`
- `.github/ISSUE_TEMPLATE/*`
- `.github/PULL_REQUEST_TEMPLATE.md`
- `.github/copilot-instructions.md`
- `docs/collaboration.md`, `docs/github-setup.md`
- Git metadata and remote configuration
- No database migration.

## Verification

- Current branch: `main`.
- Remote: `https://github.com/HoangXuyen-STEM/xuyenlab-chemistry.git`.
- Root commit: `bf149ee` (`chore(p0): establish project foundation`).
- Handoff commit pushed to remote: `64f74a7` (`docs(p0): record git foundation handoff`).
- `git ls-remote` verified `refs/heads/main` at `64f74a72e993507270b9464875c13aa4eb001137`.
- Project owner confirmed active ruleset `protect-main`, zero required approvals and status checks intentionally deferred until P1 CI defines stable check names.
- Pre-commit text scan found no private-key, database-URL or configured-secret pattern.
- `.env` and generated build/test/PDF paths are ignored.

## Environment and external services

- Git 2.43.0 available.
- Git LFS not installed.
- Git identity already configured locally as `Xuyen Hoang` with the owner's email.
- GitHub authentication and push verified successfully.

## Remaining work, known issues and risks

- Recommended labels are documented but not created remotely; this does not block P1.
- Add required status checks to `protect-main` only after P1 CI has run successfully and check names are stable.

## Plan deviations

- Git LFS is deferred rather than installed. The decision and threshold for revisiting are documented.

## Required inputs for the next task/phase

- P1.1 creates the application foundation on a task branch and opens a pull request.
- P1.2 supplies stable CI check names before the ruleset requires status checks.
