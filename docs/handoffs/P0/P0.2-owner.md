# Handoff P0.2 — Project owner

## Status

Complete

Account/resource preparation and development environment separation are complete for the P0 exit gate. Provider callbacks and secrets remain deliberately deferred to P3.

## Inputs read

- P0.2 no-code cloud/OAuth checklist provided in the project conversation.
- Secret-handling rules from `docs/contracts/storage-and-environment.md`.

## Completed

- Created private GitHub repository and granted Vercel access.
- Created Vercel account.
- Created Neon project in AWS Singapore and enabled Neon Auth.
- Created Neon child branch `development` from `production`.
- Activated Cloudflare R2 and created two development buckets.
- Confirmed `xuyenlab.com` is managed in Cloudflare.
- Created Google Cloud project with External OAuth audience.

## Decisions and assumptions

- Vercel project creation waits until P1 produces a deployable Next.js application.
- Google OAuth client creation waits until Neon Auth provides the exact redirect URI.
- Email/password configuration waits for the Phase 3 auth implementation/test.
- Development services remain separate from later production buckets/configuration.

## Files and migrations changed

- `docs/cloud-resources.md` records non-secret resource state.
- No migration was run.

## Verification

- Resource statuses were reported by the project owner.
- The private GitHub repository URL could not be fetched without authentication, which is expected for a private repository.
- No secret was supplied or recorded.

## Environment and external services

- GitHub: `HoangXuyen-STEM/xuyenlab-chemistry`.
- Neon: project `dark-lab-12774574`, AWS Singapore, Auth enabled.
- R2: `chem-assets-dev`, `chem-private-dev`.
- Google OAuth audience: External.

## Remaining work, known issues and risks

- Enabling email/password and Google OAuth too early may create incorrect callback/configuration, so those actions are deliberately deferred.
- Vercel, R2 and OAuth secrets remain uncreated/unconfigured by design.

## Plan deviations

- OAuth client/provider configuration is moved from P0.2 to P3 because the exact deployed callback URL does not exist yet. This reduces configuration error and does not block P1.

## Required inputs for the next task/phase

- P0.3 must initialize the local Git repository and configure the GitHub remote.
