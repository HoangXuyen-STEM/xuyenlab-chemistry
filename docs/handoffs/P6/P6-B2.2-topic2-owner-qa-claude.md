# Handoff P6-B2.2 — Claude Code

## Status

Complete. Records the Project Owner's real Topic 2 Part I QA decision at
its source of truth and promotes Topic 2 from `draft` to `in_review`. This
task's own PR is **not yet merged** — everything below describes work done
in this PR, not a state already integrated into `main`.

## Ownership / authorization

- **Owner decision:** Thầy Xuyên, recorded 2026-08-15. Thầy Xuyên reviewed
  Topic 2 Part I directly and made three concrete dispositions (see
  "Decisions and assumptions" below), recorded verbatim as instructed.
- **Implementer:** Claude Code, explicitly delegated by the Project Owner to
  record this decision in the repository.
- **Reviewer:** integration review before merge.
- This delegation is scoped to recording an already-made Owner decision
  faithfully — it is not a delegation of chemistry judgment, QA discretion,
  or publication authority. Every disposition, note and value below was
  specified by the task instruction verbatim or derived mechanically
  (hashes, cross-references).
- **Plan-extension note:** the written plan names P6-B2.0 (preflight/import)
  and P6-B2.1 (presentation verification); P6-B2.2 (Owner QA + lifecycle
  promotion) is not itself named in the plan text, but is the direct
  structural analogue of P6-B1.4 (Topic 24's own Owner QA step) applied to
  Topic 2, authorized by the Project Owner as this task's own instruction.

## Inputs read

- `docs/handoffs/P6/P6-B2.0-topic2-preflight-claude.md`,
  `docs/handoffs/P6/P6-B2.0-import-claude.md`,
  `docs/handoffs/P6/P6-B2.1-topic2-presentation-claude.md` (this task's
  prerequisites, merged as PR #41 and PR #43)
- `docs/handoffs/P6/P6-B1.4-claude.md` (the direct structural template for
  this task, applied to Topic 24)
- `docs/contracts/content.md` (`LessonQaRecord`, "Remediation queue")
- `scripts/validate-content/validate.py` — read in full before writing
  anything, specifically to verify the task's stated contract constraints
  against the actual validator code (see "Decisions and assumptions" below)
- `content/topics/chuyen-de-02/bang-tuan-hoan.mdx`,
  `content/qa/pending/bang-tuan-hoan.json`,
  `content/qa/pending/bang-tuan-hoan.remediation-queue.json`,
  `content/qa/import-reports/bang-tuan-hoan.failure.json`,
  `content/pilot-staging-manifest.json`
- `tests/content/topic24-owner-qa.test.ts` (direct template for this task's
  new test file)

## Decisions and assumptions

Recorded exactly as instructed, not reinterpreted:

1. **Owner reviewed Topic 2 Part I on 2026-08-15 and approved all seven QA
   checks** (scope Part I, chemistry, formulas, tables, figures, mobile,
   print) — all set `true`; `reviewer` set to the exact string `"Thầy Xuyên
(Project Owner)"`; `reviewedAt` set to the date-only value `"2026-08-15"`.
2. **`T02-S01:i6022`** (image): `status: "accepted-with-limitation"`,
   `remediationChoice: "owner-accepted-visible-fallback"` — the only
   contract-valid choice for `kind: "image"`, per
   `REMEDIATION_CHOICE_KIND["owner-accepted-visible-fallback"] == "image"`
   (`scripts/validate-content/validate.py:76-79`).
3. **`T02-S01:t7931`** (table): `status: "accepted-with-limitation"`,
   `remediationChoice: "owner-accepted-source-fidelity"` — the only
   contract-valid choice for `kind: "table"`.
4. **`T02-S01:d1402`** (drawing, blocking): recorded as **`status:
"blocked"`, `remediationChoice: "remain-blocking"`** — explicitly NOT
   `accepted-with-limitation`/`owner-accepted-visible-fallback`. Verified
   before writing: `T02-S01:d1402`'s failure-report block has a `fallback`
   object with only `reason`/`strategy` fields — no `assetPath`/`altText` —
   so it structurally cannot satisfy
   `validate_accepted_with_limitation`'s `owner-accepted-visible-fallback`
   branch (`scripts/validate-content/validate.py:356-395`, which requires
   both fields). `"blocked"`/`"remain-blocking"` is an explicitly
   pre-existing, already-supported legacy combination (comment at
   `validate.py:63-68`: _"existing legacy status/choice values
   (pending-owner-review/applied/blocked; reviewed-latex-mdx/
   reviewed-image-fallback/remain-blocking/null) are never rejected or
   rewritten by this validator"_) — not an invented value. `npm run
content:validate` passed on the first attempt with this recording; no
   contract amendment was needed and none was made.

For all three: `ownerDecision.altText`/`caption`/`reviewedLatex` stay `null`
(no remediation payload was authored); `issueId`, `sourceId`, `topic`,
`lessonSlug`, `sourceLocator`, `issueCode`, `kind`, `severity`, `message`,
`observedType`/`observedTypeEvidence`, and `previewPath` are byte-for-byte
unchanged from before this PR (verified against the untouched failure
report in `tests/content/topic2-owner-qa.test.ts`); each issue remains in
the QA record's `unresolved` array exactly as before, including the
drawing at `severity: "blocking"`; no `discussionPrompt` was added to any
item.

`approvedForPublish` stays `false` and no `publishWaiver` was added — Topic
2 is not one of the two P6.2-named exception lessons
(`P6_OWNER_APPROVED_PUBLISH_SLUGS` in `scripts/validate-content/validate.py`
still lists only `dong-hoa-hoc`/`dung-dich-va-can-bang-hoa-hoc`), and this
task does not add it to that allowlist. The drawing's blocking severity is
unresolved on `main`; the validator's `in_review` rule
(`validate.py:608-617`) requires signed `reviewer`/`reviewedAt`/checks, not
zero blocking items — T06/T08 already carry many blocking items while
`in_review`, so this is the established, already-proven pattern, not a new
exception.

MDX frontmatter `summary`/`keywords` were reworded (`"pilot-draft"` →
`"pilot-in-review"`; summary no longer claims content "vẫn chờ Chủ dự án
QA") — the same class of lifecycle-only change T24's own P6-B1.4 made. No
other frontmatter field, and no byte of the Chemistry body, table,
`ChemFigure`, or the drawing's blocking `Callout`, was touched (verified in
`tests/content/topic2-owner-qa.test.ts`). The fixture `body.mdx` under
`src/app/fixtures/pilot/chuyen-de-02/bang-tuan-hoan/` needed no
regeneration, since only the frontmatter changed (confirmed by the
pre-existing `pilot-content-regression.test.ts` byte-identity check, which
still passes).

## Files and migrations changed

- `content/qa/pending/bang-tuan-hoan.json` — all 7 `checks` → `true`;
  `reviewer`/`reviewedAt` signed; `reviewStatus` → `"in_review"`;
  `approvedForPublish` stays `false`; `unresolved`/`lessonVersion`/
  `sourceIds` unchanged.
  (`sha256:e238d9fd6ac164b6ea6141c240d5f2522eada359ea0c126396e5342e3fdb7104`)
- `content/qa/pending/bang-tuan-hoan.remediation-queue.json` — the three
  Topic 2 entries updated per the exact dispositions above; every other
  field preserved.
- `content/topics/chuyen-de-02/bang-tuan-hoan.mdx` — frontmatter `status:
draft` → `in_review`; `summary`/`keywords` reworded (lifecycle-only);
  body/table/`ChemFigure`/`Callout`/formulas untouched.
  (`sha256:3b9604dac611a84cbf278db5616ff17931a000658ec98396c5082eb9caa3f01f`)
- `content/pilot-staging-manifest.json` — Topic 2 lesson entry: `status` →
  `"in_review"`; `mdxSha256`/`qaSha256` refreshed to match the two changed
  files above; `blockingCount`/`warningCount`/provenance/
  `failureReportSha256` unchanged; `manifestVersion` unchanged (`"1.1.0"`);
  T06/T08/T24 entries confirmed byte-identical to `origin/main`.
- `tests/content/topic2-owner-qa.test.ts` — new, 21 tests: QA-record
  signing, disposition triples (2 accepted-with-limitation + 1
  blocked/remain-blocking), null remediation-payload fields, no
  `discussionPrompt`, cross-reference against the untouched failure report,
  lifecycle metadata, hash refresh, a dedicated negative check that the
  blocking drawing never satisfies `approvedForPublish`/
  accepted-with-limitation, and T06/T08/T24 preservation.
- `tests/content/pilot-content-regression.test.ts` — generalized one
  hardcoded assumption: a `"blocked"` item's `remediationChoice` was
  asserted to always equal `"reviewed-image-fallback"` (true only for T08);
  now accepts either that value or `"remain-blocking"` (Topic 2's), both
  explicitly recognized legacy choices per the validator's own comment.
- `src/features/content/pilot-manifest.test.ts` — Topic 2's expected
  `status` updated from `"draft"` to `"in_review"`.
- `src/app/fixtures/pilot/chuyen-de-02/bang-tuan-hoan/page.test.tsx` —
  rewritten from the P6-B2.0/P6-B2.1-era draft-assuming tests to the real
  in_review state: `getAllByText` for the now-multiple `"T02-S01"`
  occurrences, in_review banner (not draft), both real accepted-with-
  limitation items present with their exact issue IDs, the still-blocking
  drawing explicitly absent from that section, and no misleading
  fixed/verified/accessibility-improved copy.
- `src/features/content/p5-library/library.test.ts` — added a real-data
  test: Topic 2 now appears with `acceptedLimitationsCount: 2`.
- `src/features/content/p5-library/LibraryFixture.test.tsx` — added a
  real-data test: Topic 2's card shows "Giới hạn được lưu theo nguồn" → `2`.
- `tests/e2e/pilot-staging.spec.ts` — added two Playwright tests: Topic 2's
  two real accepted-with-limitation dispositions render with no misleading
  copy and explicitly exclude the drawing; the drawing's blocking Callout
  stays visible and traceable, unaffected by the other two dispositions.
- `tests/e2e/p5-library-search.spec.ts` — added a Playwright test proving
  Topic 2's real card shows accepted-limitations count 2.
- `docs/handoffs/P6/P6-B2.2-topic2-owner-qa-claude.md` — this handoff.
- `docs/handoffs/P6/COORDINATION.md` — records Topic 2 Owner QA complete,
  `in_review`, with 2 accepted-with-limitation items and 1 blocked
  blocking drawing; states the next gate explicitly.
- Migrations: none.

## Verification

- `npm run content:validate` — passed on the **first attempt** with the
  Owner-instructed dispositions recorded exactly as specified; no STOP
  condition was hit, `{"valid": true, "errors": []}`.
- Focused: `npx vitest run tests/content/topic2-owner-qa.test.ts
tests/content/pilot-content-regression.test.ts
src/app/fixtures/pilot/chuyen-de-02 src/features/content/p5-library
src/features/content/pilot-manifest.test.ts` — passed (21 + 8 + 5 + 27 + 6
  tests across the affected files).
- Full unit suite: `npx vitest run` — passed, **51 test files / 300 tests**.
- Focused Playwright:
  `CI=1 npx playwright test tests/e2e/pilot-staging.spec.ts
tests/e2e/p5-library-search.spec.ts --reporter=line` — passed, **25/25**
  (4 new P6-B2.2 tests plus every pre-existing regression, all
  lifecycle-derived assertions naturally picking up Topic 2's real
  promotion without further test changes).
- `npm run verify` (format:check + lint + typecheck + `vitest run` +
  `next build`) — passed: 51 test files / 300 tests, 0 lint/type/format
  errors, build succeeded (15 static pages, same single pre-existing,
  already-documented Turbopack tracing warning as every prior P6 phase).
- `npm run pdf:dry-run` — passed. Topic 2 is now eligible and generated:
  `pdf/bang-tuan-hoan/v1/3b9604da....pdf` (120,949 bytes), alongside the
  unchanged T06/T08/T24 PDFs (matching hashes to their prior-committed
  values).
- `git diff --check` — passed, no whitespace/conflict-marker issues.
- `git diff --name-status origin/main` against T06/T08/T24 content, QA,
  remediation-queue, failure-report, asset, and fixture-route paths, plus
  `scripts/import-docx`, `scripts/validate-content`, `docs/contracts`,
  `package.json`, `package-lock.json` — empty, byte-identical.

## Environment and external services

- No secret was read, printed, or changed.
- No R2, Neon, OAuth, production, public bucket, or deployment action
  occurred.

## Remaining work, known issues and risks

- **This does not authorize publication, production, R2, public access,
  Auth/Neon, or P7.** Topic 2 is `in_review`, not `published`;
  `approvedForPublish` stays `false`; no `publishWaiver` exists for it, and
  it remains ineligible for one (not a P6.2-named exception lesson). The
  normal publication rules (a future, separate, explicit Owner
  `approvedForPublish` decision) remain entirely untouched.
- **The drawing (`T02-S01:d1402`) remains blocking.** This task records the
  Owner's decision to accept the existing visible Callout fallback for
  teacher-led staging use and NOT author an SVG/ChemFigure replacement; it
  explicitly does not resolve or reclassify the item. Any future SVG/
  ChemFigure recreation, if the Owner wants one, is a separate, later,
  explicit task.
- No contract, validator, importer, PDF/R2, workflow, auth, or production
  file was changed — this PR is data-only against the already-existing
  P6-B1.3P schema and validator.
- T06/T08/T24 and every lesson outside Topic 2 were not touched.

## Plan deviations

- None in the "reassigned due to quota" sense. See "Ownership /
  authorization" above for the plan-extension naming note (P6-B2.2 is not
  itself plan-named text, but is the direct Topic-2 analogue of the
  plan-named P6-B1.4 step, authorized by the Project Owner as this task).

## Required inputs for the next task/phase

- Integration review of this PR before merge.
- After merge, per the Owner's stated sequence: any further P6 phase reads
  Topic 2 as `in_review` with 2 real Owner-accepted limitations, 1 real
  Owner-acknowledged still-blocking item, no `discussionPrompt` yet, and
  `approvedForPublish: false` — the same shape Topic 24 was in immediately
  after its own P6-B1.4.
