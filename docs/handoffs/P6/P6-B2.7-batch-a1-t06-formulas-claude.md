# Handoff P6-B2.7 — Claude Code

## Status

Complete. Applies the Project Owner's approved Batch A1 (11 OLE formula
remediations) to Chuyên đề 06 (`dong-hoa-hoc`, `T06-S01`). This is the
first content change to Topic 6 anywhere in the P6 phase; the lesson was
otherwise byte-identical since P4.5. This task's own PR is **not yet
merged**.

## Ownership / authorization

- **Owner decision:** Batch A1 — 11 named `issueId -> reviewedLatex`
  mappings, given verbatim in the task instruction.
- Claude Code implements the recording; every LaTeX mapping was supplied
  by the Owner, not authored here.
- Integration review before merge; not merged by this task.

## Prerequisites re-verified before any edit

- `main` at `4198d8d0b619cf503fc4b53b758cb513fa5091ce` (matches this
  task's stated baseline; PR #50 confirmed merged).
- All 11 target `issueId`s independently confirmed, before editing
  anything, to be real, currently `pending-owner-review` /
  `remediationChoice: null` / `severity: "blocking"` /
  `kind: "embeddedObject"` / `observedType: "formula"` entries in
  `content/qa/pending/dong-hoa-hoc.remediation-queue.json` (99 items
  total in that file), and to each appear exactly once as an untouched
  `<Callout type="warning" title="Đối tượng Word cần biên tập">` block in
  `content/topics/chuyen-de-06/dong-hoa-hoc.mdx` naming that exact id.
- Confirmed the established MDX convention for an applied
  `reviewed-latex-mdx` formula from the real T06 precedent
  (`T06-S01:e4743`): `{/* issueId */}` on its own line, a blank line,
  then the LaTeX (display `$$...$$` for a full equation — matches every
  one of these 11 mappings, all supplied already wrapped in `$$`).
- Confirmed via `tests/content/pilot-content-regression.test.ts` (the
  durable contract for this pairing) that `applied` +
  `reviewed-latex-mdx` only requires: the issue id remains somewhere in
  the MDX (the `{/* ... */}` comment satisfies this), no Callout
  containing that id remains, and `ownerDecision.decidedBy`/`decidedAt`/
  `reviewedLatex` are all truthy. No `kind` restriction applies to this
  legacy pairing (unlike the newer `reviewed-image-fallback` /
  `kind: "drawing"` pairing from P6-B2.4B).

## Decisions and assumptions

- **`ownerDecision.reviewedLatex` stores each mapping's LaTeX exactly as
  supplied**, including its own `$$...$$` delimiters, matching the exact
  shape of the pre-existing `T06-S01:e4743` entry.
- **`ownerDecision.qaNote`** set to the Owner's supplied text verbatim:
  `"Owner phê duyệt Lô A1 (2026-08-16)."` for all 11 items.
- **`altText`/`caption` stay `null`** — this is a formula recreation, not
  an image fallback; matches every existing `reviewed-latex-mdx` entry.
- **`blockingCount`/`warningCount` in the manifest stay `96`/`3`** —
  frozen historical import-time metric, unaffected by disposition
  changes (same rule established in every prior P6-B2.x task). QA
  `unresolved` also stays exactly as-is: all 11 ids remain listed at
  `severity: "blocking"`, per the established rule that `applied` items
  stay in `unresolved` at original severity (proven precedent: the 4
  pre-existing T06 `applied` items still sit in `unresolved` today).
- **Only `mdxSha256` refreshed** in the manifest — the sole file whose
  bytes changed among the manifest-tracked triple (`mdxSha256`/
  `qaSha256`/`failureReportSha256`/`sourceSha256`). QA record itself
  (`content/qa/pending/dong-hoa-hoc.json`) was not touched, so
  `qaSha256` is unchanged.
- **Manifest edit done as a single-line `Edit`, not a JSON re-dump** —
  an initial `json.dump`-based rewrite of the manifest reformatted
  unrelated compact `"sourceIds": ["T08-S01"]` arrays onto multiple
  lines across the whole file (Python's `json.dump(..., indent=2)`
  always expands arrays). Reverted and used a targeted single-field
  edit instead so the diff touches only the one changed hash.
- **Fixture `body.mdx` regenerated from the canonical MDX** by stripping
  the frontmatter block only (no other transform exists in this repo for
  this fixture; confirmed by diffing the pre-existing fixture against
  the canonical MDX's body before editing).
- **Three pre-existing tests needed updating**, not because their logic
  was wrong, but because they hard-coded T06's prior byte-identical
  state (this is the first time T06 content has changed since those
  tests were written):
  - `tests/content/remediation-queue.test.ts`: `PROCESSED_ISSUE_IDS`
    (an explicit allowlist of ids with a recorded Owner decision) grew
    to include the 11 new ids; a stray positive assertion that
    `T06-S01:e0469` was "out of [a prior] task's scope" and must remain
    an untouched Callout was corrected to a negative assertion (it no
    longer is).
  - `tests/content/topic2-owner-qa.test.ts` and
    `tests/content/topic24-owner-qa.test.ts`: both hard-code T06's full
    manifest-lesson object as a "preserved exactly" regression lock from
    an earlier, unrelated task (Topic 2/Topic 24 Owner QA). Updated only
    the `mdxSha256` field in each to the new real value; every other
    field (including T08/T24's own hard-coded objects) is untouched.
  - No validator, contract, or QA-record file was touched to make these
    pass — only the tests' own hard-coded expectations.

## Files and migrations changed

- `content/topics/chuyen-de-06/dong-hoa-hoc.mdx` — 11 `<Callout>` blocks
  removed, each replaced by `{/* issueId */}` + the approved `$$...$$`
  formula, in place. No other text changed.
- `content/qa/pending/dong-hoa-hoc.remediation-queue.json` — the 11
  target items: `status` → `"applied"`, `remediationChoice` →
  `"reviewed-latex-mdx"`, `ownerDecision` populated (`decidedBy`,
  `decidedAt: "2026-08-16"`, `reviewedLatex`, `qaNote`; `altText`/
  `caption` stay `null`). All other fields (`issueId`, `severity`,
  `sourceLocator`, `observedType`, `observedTypeEvidence`,
  `previewPath`, etc.) and all other 88 items in the file untouched.
- `content/pilot-staging-manifest.json` — `mdxSha256` for
  `chuyen-de-06/dong-hoa-hoc` refreshed to
  `e1c817886505a43b4413a56c0aa9acbb2a5bff53a9a788dc7f0c913b02197a4d`.
  Nothing else in the file changed.
- `src/app/fixtures/pilot/chuyen-de-06/dong-hoa-hoc/body.mdx` — synced
  to the canonical MDX body (frontmatter stripped), same 11 blocks
  changed.
- `tests/content/remediation-queue.test.ts`,
  `tests/content/topic2-owner-qa.test.ts`,
  `tests/content/topic24-owner-qa.test.ts` — updated stale hard-coded
  expectations, as described above.
- `docs/handoffs/P6/P6-B2.7-batch-a1-t06-formulas-claude.md` — this
  handoff.
- Migrations: none. **Not changed:** `docs/contracts/content.md`,
  `scripts/validate-content/validate.py`,
  `content/qa/pending/dong-hoa-hoc.json` (QA record itself),
  `content/qa/import-reports/dong-hoa-hoc.failure.json`, any T02/T08/T24
  content/QA/remediation-queue file, `package.json`, `package-lock.json`,
  any auth/R2/workflow file, `docs/handoffs/P6/COORDINATION.md`.

## Verification

- `npm run content:validate` (`python3 scripts/validate-content/validate.py --json`)
  — **PASS**, `{"valid": true, "errors": []}`.
- Full unit suite (`npx vitest run`) — **PASS**, 51 test files / 312
  tests (4 initially failed on stale hard-coded T06 state; fixed by
  updating those tests' own expectations as described above, then
  re-ran clean).
- `npm run build` — **PASS**, same pre-existing Turbopack tracing
  warning as every prior P6 phase; no build errors.
- Focused Playwright (`tests/e2e/pilot-staging.spec.ts`) — **PASS**,
  19/19, including the blocking-fallback-visibility test (T06 still has
  85 untouched blocking Callouts after this batch) and every Topic
  2/24/6/8 assertion.
- `git diff --name-status origin/main` — confirmed exactly 7 files
  changed: the MDX, the remediation queue, the manifest, the fixture,
  and the 3 test files listed above. No T02/T08/T24 content, QA, or
  remediation-queue file changed.

## Environment and external services

- No secret was read, printed, or changed.
- No R2, Neon, OAuth, production, public bucket, or deployment action
  occurred.

## Remaining work, known issues and risks

- **85 of Topic 6's 96 blocking items remain unresolved Callouts.** This
  batch (A1, 11 items) is a partial pass over a 99-item queue; the
  remaining formula/table/drawing items still need their own future
  Owner-approved batches.
- **Not a publication or governance change.** `dong-hoa-hoc` keeps its
  existing `approvedForPublish: true` / P6.2 `publishWaiver`
  (`unresolvedBlockingCount: 96`, unchanged — this batch does not touch
  the QA record or reduce the live blocking count, since `unresolved`
  stays frozen by design). `status` stays `in_review`; nothing here
  authorizes production or public access.
- No further P6 work is authorized by this task.

## Plan deviations

- Fixed 3 pre-existing tests that hard-coded T06 as byte-unchanged —
  necessary because this is the first legitimate T06 content edit since
  those tests were written; not a scope expansion, since only the
  affected hash/assertions were touched (see "Decisions and
  assumptions").
- No other deviation. Implemented exactly the 11 named mappings, no
  more, no less.

## Required inputs for the next task/phase

- Integration review and Owner merge decision for this PR.
- After merge: Batch A2 (or later) would apply further Owner-approved
  mappings from the remaining 85 T06 blocking items, following the same
  pattern established here.
