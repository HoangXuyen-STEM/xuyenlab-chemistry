# Handoff P6-B2.12 — Claude Code

## Status

Complete. Applies the Project Owner's approved Batch A6-FINAL (all 36
remaining OLE formula remediations) to Chuyên đề 06 (`dong-hoa-hoc`,
`T06-S01`), reviewed by the Owner against the rendered HTML on
2026-08-16. This completes **100% of T06's formula items** (6 batches
total: the original P4.5 sample of 4, plus Batches A1-A5 at 11 each,
plus this batch's 36). This task's own PR is **not yet merged**.

**This does NOT close T06's whole remediation queue.** One drawing
(`T06-S01:d6703`, blocking) and three table items (`t2740`, `t3041`,
`t6560`, all warning) are explicitly out of scope for the
`reviewed-latex-mdx` pairing and remain `pending-owner-review` after
this batch — see "Remaining work" below.

## Ownership / authorization

- **Owner decision:** Batch A6-FINAL — 36 named `issueId ->
reviewedLatex` mappings, given verbatim in the task instruction,
  approved after the Owner reviewed the rendered HTML.
- Claude Code implements the recording; every LaTeX mapping was supplied
  by the Owner, not authored here.
- Integration review before merge; not merged by this task.

## Prerequisites re-verified before any edit

- `main` at `a1eb27df824684aaecbc45987f8e040e0baf1f08` (matches this
  task's stated baseline; PR #55 — P6-B2.11 Batch A5 — confirmed
  merged).
- **Independently verified the task's "36 remaining, 100% of formula
  items" claim before implementing** (per this session's established
  practice of never trusting a task's own count claim blindly): queried
  the real remediation queue and found exactly 40 `pending-owner-review`
  items total — 36 with `observedType: "formula"` (all `severity:
"blocking"`, `kind: "embeddedObject"`), 1 drawing (`d6703`,
  `severity: "blocking"`) and 3 tables (`t2740`/`t3041`/`t6560`, all
  `severity: "warning"`). Diffed the task's 36 named `issueId`s against
  the real set of 36 pending formula ids: **exact match, zero
  discrepancy**.
- All 36 confirmed to each appear exactly once as an untouched
  `<Callout>` block naming that id in
  `content/topics/chuyen-de-06/dong-hoa-hoc.mdx`, none overlapping the
  95 items already `applied` from the P4.5 sample and Batches A1-A5.
- Same `{/* issueId */}` + `$$...$$` convention re-confirmed from the
  Batch A1-A5 precedents and the original `T06-S01:e4743` precedent.
- `e0189`'s pattern repeats: this batch's `e7784`/`e2971`/`e6018` are new
  distinct equations (methyl acetate hydrolysis, acetic acid
  neutralization, HCl neutralization for a different worked example on
  reaction-rate-by-titration), not duplicates of earlier batches'
  saponification equation — checked individually, not assumed.
- LaTeX mappings applied exactly as supplied by the Owner, without
  independent chemical correction (several of this batch's items are
  short symbolic/proportionality statements, e.g. `e3386`: `a \propto
(V_\infty - V_0)`, and two items — `e0904`, `e5843` — are plain
  Vietnamese sentences wrapped in `\text{}` rather than a chemistry
  formula; applied verbatim as given).

## Decisions and assumptions

Same as P6-B2.7 through P6-B2.11, applied to this batch's 36 ids:

- `ownerDecision.reviewedLatex` stores each mapping's LaTeX exactly as
  supplied, including its own `$$...$$` delimiters.
- `ownerDecision.qaNote` set verbatim: `"Owner phê duyệt Lô A6-FINAL
(2026-08-16)."` for all 36 items.
- `altText`/`caption` stay `null` (formula recreation, not an image
  fallback).
- `blockingCount`/`warningCount` in the manifest stay `96`/`3` (frozen
  historical metric — unaffected even though the live blocking count in
  the queue is now down to 1 unresolved formula-adjacent item, the
  drawing); QA `unresolved` stays exactly as-is — all 36 ids remain
  listed at `severity: "blocking"`.
- Only `mdxSha256` refreshed in the manifest, via a targeted single-field
  `Edit` (not a JSON re-dump, per the pitfall already found and avoided
  in P6-B2.7).
- Fixture `body.mdx` regenerated the same way as prior batches:
  canonical MDX with its frontmatter block stripped.
- **Same 3 pre-existing tests needed the same class of update as
  P6-B2.7 through P6-B2.11**, for the same reason (they hard-code T06's
  manifest hash / processed-ids allowlist as a "preserved exactly"
  invariant from an earlier, unrelated task):
  - `tests/content/remediation-queue.test.ts`: `PROCESSED_ISSUE_IDS`
    grew to include this batch's 36 ids (kept as an explicit allowlist,
    consistent with every prior batch's pattern, rather than switching
    to a derived check — a stronger, deliberate guarantee against an
    accidental extra `applied` item, not just a tautology); doc comment
    rewritten to summarize all 6 batches and explicitly name the 4 items
    (1 drawing + 3 tables) that remain out of scope.
  - `tests/content/topic2-owner-qa.test.ts` and
    `tests/content/topic24-owner-qa.test.ts`: updated only the
    hard-coded `mdxSha256` field for T06 to the new real value. T08/T24's
    own hard-coded objects, and every other field of T06's, are
    untouched.
  - No validator, contract, or QA-record file touched.

## Files and migrations changed

- `content/topics/chuyen-de-06/dong-hoa-hoc.mdx` — 36 `<Callout>` blocks
  removed, each replaced by `{/* issueId */}` + the approved `$$...$$`
  formula, in place. The one remaining Callout in the file (the drawing
  `T06-S01:d6703`) is untouched. No other text changed.
- `content/qa/pending/dong-hoa-hoc.remediation-queue.json` — this
  batch's 36 items: `status` → `"applied"`, `remediationChoice` →
  `"reviewed-latex-mdx"`, `ownerDecision` populated. All other fields
  and all other 63 items in the file untouched (95 `applied` total now;
  4 still `pending-owner-review`: the drawing and 3 tables).
- `content/pilot-staging-manifest.json` — `mdxSha256` for
  `chuyen-de-06/dong-hoa-hoc` refreshed to
  `ee10b5f241e3537f353be670d77ccdb5e3dfc2400f8502edd0b51be41c3b5852`.
  Nothing else in the file changed.
- `src/app/fixtures/pilot/chuyen-de-06/dong-hoa-hoc/body.mdx` — synced
  to the canonical MDX body (frontmatter stripped), same 36 blocks
  changed.
- `tests/content/remediation-queue.test.ts`,
  `tests/content/topic2-owner-qa.test.ts`,
  `tests/content/topic24-owner-qa.test.ts` — updated stale hard-coded
  expectations, as described above.
- `docs/handoffs/P6/P6-B2.12-batch-a6-final-t06-formulas-claude.md` —
  this handoff.
- Migrations: none. **Not changed:** `docs/contracts/content.md`,
  `scripts/validate-content/validate.py`,
  `content/qa/pending/dong-hoa-hoc.json` (QA record itself),
  `content/qa/import-reports/dong-hoa-hoc.failure.json`, any T02/T08/T24
  content/QA/remediation-queue file, `package.json`, `package-lock.json`,
  any auth/R2/workflow file, `docs/handoffs/P6/COORDINATION.md`.

## Verification

- `npm run content:validate` — **PASS**, `{"valid": true, "errors": []}`.
- Full unit suite (`npx vitest run`) — **PASS**, 51 test files / 312
  tests (3 initially failed on stale hard-coded T06 state — same 3
  files as prior batches — fixed the same way, then re-ran clean).
- `npm run build` — **PASS**, no errors.
- Focused Playwright (`tests/e2e/pilot-staging.spec.ts`) — **PASS**,
  19/19, including the blocking-fallback-visibility test (T06's one
  remaining Callout — the drawing — still renders as a visible blocking
  fallback with a stable issue ID).
- `git diff --name-status origin/main` — confirmed exactly 7 files
  changed (the same set as prior batches): the MDX, the remediation
  queue, the manifest, the fixture, and the 3 test files. No
  T02/T08/T24 content, QA, or remediation-queue file changed.
- Directly confirmed in the MDX: zero remaining
  `<Callout type="warning" title="Đối tượng Word cần biên tập">` blocks
  (the formula-fallback title); exactly one `<Callout>` of any kind
  remains, and it is the drawing's (`title="Hình vẽ Word cần biên
tập"`, naming `T06-S01:d6703`).

## Environment and external services

- No secret was read, printed, or changed.
- No R2, Neon, OAuth, production, public bucket, or deployment action
  occurred.

## Remaining work, known issues and risks

- **T06's remediation queue is NOT fully closed.** 4 of 99 items remain
  `pending-owner-review`:
  - `T06-S01:d6703` (drawing, `severity: "blocking"`) — out of scope for
    the `reviewed-latex-mdx` pairing; would need its own candidate-asset
    process, the same class of work done for Topic 2's `T02-S01:d1402`
    in P6-B2.4A/B, if the Owner wants it resolved.
  - `T06-S01:t2740`, `t3041`, `t6560` (tables, `severity: "warning"`) —
    also out of scope for this pairing; a table-review disposition
    (`accepted-with-limitation`/`owner-accepted-source-fidelity`, the
    P6-B1.3P vocabulary already used for Topic 2/24's table items) would
    be the applicable path if the Owner wants them dispositioned.
- **`blockingCount: 96` in the manifest is unaffected** and stays frozen
  — it is documented as a historical import-time metric, not live
  remediation state, matching the rule already established for every
  other lesson in this phase.
- **Not a publication or governance change.** `dong-hoa-hoc` keeps its
  existing `approvedForPublish: true` P6.2 `publishWaiver`
  (`unresolvedBlockingCount: 96`, unchanged — `unresolved` stays frozen
  by design). `status` stays `in_review`; nothing here authorizes
  production or public access.
- No further P6 work is authorized by this task.

## Plan deviations

- Fixed the same 3 pre-existing tests as prior batches, for the same
  reason (T06's manifest hash / processed-ids allowlist changed
  legitimately). No scope expansion beyond that.
- No other deviation. Implemented exactly the 36 named mappings, no
  more, no less — did not touch the drawing or table items even though
  they were visible in the same MDX region, since they were not part of
  this batch's authorized mapping list.

## Required inputs for the next task/phase

- Integration review and Owner merge decision for this PR.
- After merge: T06's formula-remediation work is complete. Any future
  work on the drawing (`d6703`) or the 3 table warnings would need its
  own fresh Owner decision and scoped task, following the same pattern
  already used for Topic 2's drawing/table items.
