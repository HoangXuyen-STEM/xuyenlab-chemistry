# Handoff P6-B2.0 (import) — Claude Code

## Status

Complete. Imports Topic 2 Part I (`T02-S01`) to `draft`, using the same
incremental importer already used for Topic 24 (P6-B1.1). This is the first
task since P6-B1.1 to add a new lesson to canonical `content/`.

## Ownership / authorization

- Owner: Thầy Xuyên (Project Owner). Authorized 2026-08-15, after reviewing
  `docs/handoffs/P6/P6-B2.0-topic2-preflight-claude.md`'s findings and
  verdict (RECOMMEND IMPORT).
- Implementer: Claude Code, explicitly delegated in place of the plan's
  normal owner (Copilot), due to Copilot quota unavailability — same
  delegation basis as the preflight step; see that handoff's "Ownership /
  authorization" section for the full record.
- Excluded by the Owner's instruction and respected here: no
  `approvedForPublish`, no resolution of the blocking item, no chemistry
  decision, no change to T06/T08/T24 canonical content.

## Inputs read

- `docs/handoffs/P6/P6-B2.0-topic2-preflight-claude.md` (this task's
  preflight, same session)
- `docs/handoffs/P6/P6-B1.1-copilot.md` (template for this exact
  import-to-draft pattern, used for Topic 24)
- `scripts/import-docx/pilot_import.py` (the incremental importer)
- `content/topics.ts`, `content/pilot-staging-manifest.json` (existing
  registration/manifest shape)

## Completed

- Ran a clean `npm ci --ignore-scripts` in an isolated worktree
  (`/home/hoang-xuyen/Projects/HSG-Chemistry/HSG-11-p6-b2-0`, branch
  `phase/p6-b2-0-topic2-import`, based on `origin/main` at `5d229b9`) to
  restore locked dependencies, required by the importer's Prettier step. No
  dependency version was changed; the same pre-existing 8-advisory
  (7 moderate, 1 critical) `npm audit` state already noted in
  `P6-B1.5-codex.md` was reproduced, not introduced.
- Ran the existing incremental importer directly against real
  `content/`, exactly as documented for Topic 24 in
  `docs/handoffs/P6/P6-B1.1-copilot.md`:
  ```
  python3 scripts/import-docx/pilot_import.py \
    --source "2. Chuyen de 2.Bảng tuần hoàn. Phan Thị Hà.ok.docx" \
    --source-id T02-S01 --topic 2 --slug bang-tuan-hoan --title "Bảng tuần hoàn"
  ```
  Result: `{"result": "updated", ...}`, exit 0.
- Registered Topic 2 in `content/topics.ts`
  (`{ slug: "chuyen-de-02", title: "Bảng tuần hoàn", order: 2 }`), matching
  the existing entries' shape (title without a "Chuyên đề N:" prefix, same
  convention as Topic 24's `"Phân bón hóa học"`).
- Confirmed the importer's incremental merge preserved every existing
  manifest lesson and asset entry byte-for-byte; only new keys were added to
  `content/pilot-staging-manifest.json` (verified via `git diff`, no `-`
  lines outside the new block).
- Fixed one pre-existing hardcoded test assumption
  (`src/features/content/pilot-manifest.test.ts`: `"lists all three manifest
  lessons"` → `"lists all four manifest lessons"`, `toHaveLength(3)` →
  `toHaveLength(4)`) and added one new lesson-specific test asserting Topic
  2's real provenance and QA counts (`sourceId: "T02-S01"`,
  `blockingCount: 1`, `warningCount: 2`, `status: "draft"`). No other test
  file required a change — the full suite (267 tests) passed with only this
  one fix, including tests that derive lesson/lifecycle counts from the real
  manifest file dynamically rather than hardcoding them.

### Topic 2 measured output (matches the preflight prediction exactly)

- Source: `T02-S01` — `2. Chuyen de 2.Bảng tuần hoàn. Phan Thị Hà.ok.docx`
  (`sha256:a99d3a4d1d0a2bab049d354d15159fb3c4f8d0763b87fa5bd19c645c5a5325a3`)
- Scope: Part I only (42 blocks; importer stops before the Part II heading)
- Failure report blocks: 42 total; 39 semantic; 3 fallback; 0 omitted
- Unresolved: **1 blocking, 2 warning**
  - `EXTRACTED_IMAGE_REQUIRES_REVIEW` (warning) — extracted PNG, needs
    Owner-authored alt text/visual review (`T02-S01:i6022`)
  - `DOCX_TABLE_REQUIRES_VISUAL_REVIEW` (warning) — flattened summary table,
    all cells placeholder `?` values (`T02-S01:t7931`)
  - `UNSUPPORTED_NON_IMAGE_DRAWING` (blocking) — native arrow AutoShape with
    no extractable image relationship (`T02-S01:d1402`)
- Assets: **1** content-addressed PNG file, 16,887 bytes
  (`public/staging-assets/lessons/76/76e8a9324baf57ed03154de9be1ba748b436c537ee387837660ebc128f68492d.png`)
- Chemistry, formula, table fidelity, image meaning/alt text, mobile and
  print: **UNVERIFIED**, intentionally left for a future presentation task
  and Owner QA — same division of labor as T24's P6-B1.1 → P6-B1.2 →
  P6-B1.4 sequence.

## Decisions and assumptions

- Used slug `bang-tuan-hoan` and title `"Bảng tuần hoàn"`, mechanically
  derived from the source's own section heading, with the topic-number
  prefix stripped for `content/topics.ts` — same convention already used for
  Topic 24 (`"phan-bon-hoa-hoc"` / `"Phân bón hóa học"`).
  - Actively considered and rejected: `"Chuyên đề 2: Bảng tuần hoàn"`
    verbatim from the DOCX title paragraph — every existing `topics.ts`
    entry uses the bare topic name, not a "Chuyên đề N:" prefix, so this
    would have been an inconsistent one-off.
- Did not infer chemical meaning anywhere. Extracted text is source text;
  the table's placeholder `?` cells and the arrow's un-rendered shape remain
  visible and machine-readable in the failure report, QA record and
  remediation queue exactly as the importer produced them.
- Did not attempt to resolve the blocking `UNSUPPORTED_NON_IMAGE_DRAWING`
  item — out of scope per the Owner's explicit instruction.

## Files and migrations changed

- `content/topics.ts` — register Topic 2.
- `content/pilot-staging-manifest.json` — append the T02 draft lesson entry
  and its one PNG asset; every existing lesson/asset entry unchanged.
- `content/topics/chuyen-de-02/bang-tuan-hoan.mdx` — canonical draft, Part I
  only (`sha256:54ec82480d53176910645b3540f75757de7090db72eb783a0ae9ffca511fbf79`).
- `content/qa/import-reports/bang-tuan-hoan.failure.json` — machine-readable
  report (`sha256:5695e1e999c05fb12c29cc46e35d8543965641b566658db784b5e6efd70350f8`).
- `content/qa/pending/bang-tuan-hoan.json` — unsigned pending QA record
  (`sha256:d63e9ac166cea015253fe6ac21c2ffb0aa216fc95cefde91f6eb4385c16272f3`).
- `content/qa/pending/bang-tuan-hoan.remediation-queue.json` — three pending
  items, no Owner decision yet
  (`sha256:e150cb6f30ed7cfe6a8bfb4eee2c2fb1fed2780fb6d6bed702d7f4bda25a5126`).
- `public/staging-assets/lessons/76/76e8a9324baf57ed03154de9be1ba748b436c537ee387837660ebc128f68492d.png`
  — extracted content-addressed source image (16,887 bytes).
- `src/features/content/pilot-manifest.test.ts` — generalized the
  hardcoded-3 assumption to 4; added a Topic 2 provenance/QA-count test.
- `docs/handoffs/P6/P6-B2.0-topic2-preflight-claude.md` — preflight handoff
  (same task set, written first per the Owner's instruction).
- `docs/handoffs/P6/P6-B2.0-import-claude.md` — this handoff.
- Migrations: none. No ADR, contract, validator, importer script, package,
  lockfile, workflow, auth or production file was changed.

## Verification

- `npm run content:validate` — passed, `{"valid": true, "errors": []}`.
- Full unit suite: `npx vitest run` — passed, **48 test files / 267 tests**
  (266 pre-existing + 1 new).
- `npm run verify` (format:check + lint + typecheck + `vitest run` +
  `next build`) — passed: same 267 tests, 0 lint/type/format errors, build
  succeeded (14 static pages; same single pre-existing, already-documented
  Turbopack tracing warning as every prior P6 phase). No static fixture page
  was generated for Topic 2, consistent with its `draft` status — the same
  behavior T24 had before its own promotion to `in_review`.
- Focused Playwright (`tests/e2e/pilot-staging.spec.ts`,
  `tests/e2e/p5-library-search.spec.ts`) — passed, **21/21**, unchanged from
  before this import; Topic 2 correctly does not appear in either spec since
  both are scoped to `in_review` lessons and Topic 2 is `draft`.
- `npm run pdf:dry-run` — passed. Generated exactly the same three
  `in_review` PDFs as before this import (`dong-hoa-hoc` 232,163 bytes,
  `dung-dich-va-can-bang-hoa-hoc` 551,281 bytes, `phan-bon-hoa-hoc` 179,304
  bytes); Topic 2 (`draft`) correctly excluded.
- `git diff --check` — passed, no whitespace/conflict-marker issues.
- T06/T08/T24 immutability, confirmed byte-identical against `origin/main`:
  `git diff --exit-code origin/main -- content/topics/chuyen-de-06 content/topics/chuyen-de-08 content/topics/chuyen-de-24 content/qa/pending/dong-hoa-hoc.json content/qa/pending/dung-dich-va-can-bang-hoa-hoc.json content/qa/pending/phan-bon-hoa-hoc.json content/qa/pending/dong-hoa-hoc.remediation-queue.json content/qa/pending/dung-dich-va-can-bang-hoa-hoc.remediation-queue.json content/qa/pending/phan-bon-hoa-hoc.remediation-queue.json content/qa/import-reports/dong-hoa-hoc.failure.json content/qa/import-reports/dung-dich-va-can-bang-hoa-hoc.failure.json content/qa/import-reports/phan-bon-hoa-hoc.failure.json public/staging-assets`
  — exit 0, byte-identical.

## Environment and external services

- No secret was read, printed or changed.
- No R2, Neon, OAuth, production, public bucket or deployment action
  occurred.
- `npm ci --ignore-scripts` restored locked dependencies only in this
  disposable worktree; no lockfile or dependency version changed.

## Remaining work, known issues and risks

- **This does not authorize publication, promotion, or QA sign-off.** Topic
  2 is `draft`, not `in_review`; `approvedForPublish` is `false`; no
  `reviewer`/`reviewedAt` is set; all seven QA checks are `false`.
- The blocking item (`UNSUPPORTED_NON_IMAGE_DRAWING`, the arrow AutoShape)
  and both warning items (image alt text, table visual review) remain
  entirely unresolved, exactly as the importer produced them — this task
  does not attempt remediation.
- Next steps mirror T24's own sequence: a presentation/mechanical-fix task
  (P6-B1.2 equivalent), then Owner Part I QA and a lifecycle promotion
  decision (P6-B1.4 equivalent) — neither has happened for Topic 2 yet.
- No contract, validator, importer, PDF/R2, workflow, or auth file was
  changed — this PR is content-only, using the already-existing importer.

## Plan deviations

- Owner delegated both the preflight and this import step to Claude Code in
  place of the plan's normal owner (Copilot), due to Copilot quota
  unavailability. Recorded in full in the preflight handoff's "Ownership /
  authorization" section; this import step inherits the same basis.

## Required inputs for the next task/phase

- Codex/integration review of this PR before merge.
- After merge: a presentation task for Topic 2's mechanical MDX/component
  fixes (parallel to P6-B1.2), then Owner Part I QA (parallel to P6-B1.4).
  Neither is authorized by this task.
