# Handoff P6-B2.8 — Claude Code

## Status

Complete. Applies the Project Owner's approved Batch A2 (11 OLE formula
remediations) to Chuyên đề 06 (`dong-hoa-hoc`, `T06-S01`), reviewed by
the Owner against the rendered HTML on 2026-08-16. This is the second
content batch applied to Topic 6 (after P6-B2.7's Batch A1). This
task's own PR is **not yet merged**.

## Ownership / authorization

- **Owner decision:** Batch A2 — 11 named `issueId -> reviewedLatex`
  mappings, given verbatim in the task instruction, approved after the
  Owner reviewed the rendered HTML.
- Claude Code implements the recording; every LaTeX mapping was supplied
  by the Owner, not authored here.
- Integration review before merge; not merged by this task.

## Prerequisites re-verified before any edit

- `main` at `1f8de1e3722a5f70047fe20bb338b8cabcae282a` (matches this
  task's stated baseline; PR #51 — P6-B2.7 Batch A1 — confirmed merged).
- All 11 target `issueId`s independently confirmed, before editing
  anything, to be real, currently `pending-owner-review` /
  `remediationChoice: null` / `severity: "blocking"` /
  `kind: "embeddedObject"` / `observedType: "formula"` entries in
  `content/qa/pending/dong-hoa-hoc.remediation-queue.json` (99 items
  total; 15 already `applied` from Batch A1 and the earlier P4.5
  precedent, none overlapping this batch's 11), and to each appear
  exactly once as an untouched `<Callout>` block naming that id in
  `content/topics/chuyen-de-06/dong-hoa-hoc.mdx`.
- Same `{/* issueId */}` + `$$...$$` convention re-confirmed from the
  Batch A1 precedent (P6-B2.7) and the original `T06-S01:e4743`
  precedent.
- Two of this batch's mappings (`e4184` and `e7002`) share the identical
  LaTeX `$$v = k C_A^a C_B^b$$` — checked and confirmed intentional: the
  source text states the same general rate-law expression twice at two
  different points in the lesson (first in general form, restated later
  once the section's variable names align), not a data-entry error.

## Decisions and assumptions

Same as P6-B2.7 (Batch A1), applied to this batch's 11 ids:

- `ownerDecision.reviewedLatex` stores each mapping's LaTeX exactly as
  supplied, including its own `$$...$$` delimiters.
- `ownerDecision.qaNote` set verbatim: `"Owner phê duyệt Lô A2
(2026-08-16)."` for all 11 items.
- `altText`/`caption` stay `null` (formula recreation, not an image
  fallback).
- `blockingCount`/`warningCount` in the manifest stay `96`/`3` (frozen
  historical metric); QA `unresolved` stays exactly as-is — all 11 ids
  remain listed at `severity: "blocking"`.
- Only `mdxSha256` refreshed in the manifest, via a targeted single-field
  `Edit` (not a JSON re-dump — P6-B2.7 already established that
  `json.dump` reformats unrelated compact arrays elsewhere in this
  file).
- Fixture `body.mdx` regenerated the same way as P6-B2.7: canonical MDX
  with its frontmatter block stripped.
- **Same 3 pre-existing tests needed the same class of update as
  P6-B2.7**, for the same reason (they hard-code T06's manifest hash /
  processed-ids allowlist as a "preserved exactly" invariant from an
  earlier, unrelated task):
  - `tests/content/remediation-queue.test.ts`: `PROCESSED_ISSUE_IDS`
    grew to include this batch's 11 ids; doc comment updated to name
    both P6-B2.7 and P6-B2.8.
  - `tests/content/topic2-owner-qa.test.ts` and
    `tests/content/topic24-owner-qa.test.ts`: updated only the
    hard-coded `mdxSha256` field for T06 to the new real value (now
    reflecting both Batch A1 and Batch A2). T08/T24's own hard-coded
    objects, and every other field of T06's, are untouched.
  - No validator, contract, or QA-record file touched.

## Files and migrations changed

- `content/topics/chuyen-de-06/dong-hoa-hoc.mdx` — 11 `<Callout>` blocks
  removed, each replaced by `{/* issueId */}` + the approved `$$...$$`
  formula, in place. No other text changed.
- `content/qa/pending/dong-hoa-hoc.remediation-queue.json` — this
  batch's 11 items: `status` → `"applied"`, `remediationChoice` →
  `"reviewed-latex-mdx"`, `ownerDecision` populated. All other fields
  and all other 88 items in the file untouched.
- `content/pilot-staging-manifest.json` — `mdxSha256` for
  `chuyen-de-06/dong-hoa-hoc` refreshed to
  `d86c4526d539ea669a9e81e6373ec1be97409d7ec3163038ca532364fe5795ed`.
  Nothing else in the file changed.
- `src/app/fixtures/pilot/chuyen-de-06/dong-hoa-hoc/body.mdx` — synced
  to the canonical MDX body (frontmatter stripped), same 11 blocks
  changed.
- `tests/content/remediation-queue.test.ts`,
  `tests/content/topic2-owner-qa.test.ts`,
  `tests/content/topic24-owner-qa.test.ts` — updated stale hard-coded
  expectations, as described above.
- `docs/handoffs/P6/P6-B2.8-batch-a2-t06-formulas-claude.md` — this
  handoff.
- Migrations: none. **Not changed:** `docs/contracts/content.md`,
  `scripts/validate-content/validate.py`,
  `content/qa/pending/dong-hoa-hoc.json` (QA record itself),
  `content/qa/import-reports/dong-hoa-hoc.failure.json`, any T02/T08/T24
  content/QA/remediation-queue file, `package.json`, `package-lock.json`,
  any auth/R2/workflow file, `docs/handoffs/P6/COORDINATION.md`.

## Verification

- `npm run content:validate` — **PASS**, `{"valid": true, "errors": []}`.
- Full unit suite (`npx vitest run`) — **PASS**, 51 test files / 312
  tests (3 initially failed on stale hard-coded T06 state — same 3
  files as P6-B2.7 — fixed the same way, then re-ran clean).
- `npm run build` — **PASS**, no errors.
- Focused Playwright (`tests/e2e/pilot-staging.spec.ts`) — **PASS**,
  19/19, including the blocking-fallback-visibility test (T06 still has
  70 untouched blocking Callouts after this batch).
- `git diff --name-status origin/main` — confirmed exactly 7 files
  changed (the same set as P6-B2.7): the MDX, the remediation queue, the
  manifest, the fixture, and the 3 test files. No T02/T08/T24 content,
  QA, or remediation-queue file changed.

## Environment and external services

- No secret was read, printed, or changed.
- No R2, Neon, OAuth, production, public bucket, or deployment action
  occurred.

## Remaining work, known issues and risks

- **70 of Topic 6's 96 blocking items remain unresolved Callouts**
  (26 now `applied`: 4 pre-existing from P4.5, 11 from Batch A1, 11 from
  this batch). This batch is a partial pass over the 99-item queue; the
  remaining formula/table/drawing items still need their own future
  Owner-approved batches.
- **Not a publication or governance change.** `dong-hoa-hoc` keeps its
  existing `approvedForPublish: true` P6.2 `publishWaiver`
  (`unresolvedBlockingCount: 96`, unchanged — `unresolved` stays frozen
  by design). `status` stays `in_review`; nothing here authorizes
  production or public access.
- No further P6 work is authorized by this task.

## Plan deviations

- Fixed the same 3 pre-existing tests as P6-B2.7, for the same reason
  (T06's manifest hash / processed-ids allowlist changed legitimately).
  No scope expansion beyond that.
- No other deviation. Implemented exactly the 11 named mappings, no
  more, no less.

## Required inputs for the next task/phase

- Integration review and Owner merge decision for this PR.
- After merge: a future Batch A3 (or later) would apply further
  Owner-approved mappings from T06's remaining blocking items, following
  the same pattern established in P6-B2.7 and this task.
