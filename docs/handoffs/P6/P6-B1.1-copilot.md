# Handoff P6-B1.1 — GitHub Copilot GPT-5.3-Codex role

## Status

Complete. Copilot implementation completed locally first; the integration owner
subsequently committed and opened the review PR. The PR is not merged.

## Post-handoff integration update

- Integration commit: `f5ffa9be0c5543c3bde0a8ff8104f84a1a7a7846`
  (`feat(p6-b1.1): import topic 24 incrementally`).
- Branch: `phase/p6-b1-1-topic24`.
- Review PR: [#32](https://github.com/HoangXuyen-STEM/xuyenlab-chemistry/pull/32),
  opened by the integration owner after this task's local completion.
- GitHub CI Validate: PASS — 2026-08-14 run
  `31780361005`.
- Vercel deployment: PASS; Vercel Preview Comments: PASS.
- This update records external verification that was unavailable at the moment of
  the original local handoff. It does not assert that PR #32 has been merged.

## Inputs read

- `AGENTS.md`
- `KE_HOACH_XUYENLAB_CHEMISTRY.md`
- `docs/adr/0002-content-lifecycle.md`
- `docs/adr/0004-agent-ownership-and-handoffs.md`
- `docs/contracts/content.md`
- `docs/contracts/handoff.md`
- `docs/handoffs/P5/SUMMARY.md`
- `docs/handoffs/P6/READINESS.md`
- `docs/handoffs/P6/COORDINATION.md`
- `docs/handoffs/P6/P6-B1.0-claude.md`
- `docs/source-manifest.csv` and `docs/source-inventory.md`
- Existing importer, validator, manifest, pilot QA/remediation records and focused tests.
- Task instruction from the integration owner, including allowed/excluded scope and
  Definition of Done.

## Completed

- Started from exact merged P6-B1.0 baseline
  `07e73d00e222b08bb1653cfec2554fb72c1af97e` in the isolated branch/worktree
  `phase/p6-b1-1-topic24` / `/tmp/xuyenlab-p6-b1-1`.
- Parameterized `pilot_import.py` for the explicit argument set `--source`,
  `--source-id`, `--topic`, `--slug`, `--title`; partial argument sets fail.
- Generalized source authorization to accept the existing manifest disposition
  `import_part_i`; source ID, repository path and topic continue to be validated
  against `docs/source-manifest.csv` by the converter prototype.
- Replaced whole-manifest installation with an incremental merge for requested
  lessons and assets. Unrelated lessons, QA, report files and assets are preserved.
  A requested `in_review` lesson is refused before generation and cannot be forced.
  Recorded-hash drift on a requested draft writes the existing diff report and
  refuses before changing managed content unless the existing explicit
  `--force --backup-dir` recovery path is used.
- Registered Topic 24 and imported only `T24-S01` as
  `chuyen-de-24/phan-bon-hoa-hoc`, status `draft`.
- Confirmed the structural source boundary directly in DOCX XML: the single Part I
  marker is body block 3 and the first following Part II marker is block 15. The MDX
  contains blocks 3–14 and does not contain the Part II heading or later sections.
- Produced canonical MDX, pending QA, failure report, remediation queue and two
  content-addressed JPEG assets. Every QA check is initially `false`; reviewer and
  reviewedAt are `null`; `approvedForPublish` is `false`; no `publishWaiver` exists.
- Generalized three pre-existing test assumptions that incorrectly required exactly
  two/all-`in_review` manifest lessons, then added incremental import, schema,
  provenance, mixed-status, byte-preservation, idempotency and drift regressions.

### Topic 24 measured output

- Source: `T24-S01` — `24. Chuyen de 24_ Phan bon hoa hoc_OK.docx`
- Scope: Part I only (body blocks 3–14; Part II begins at block 15)
- Failure report blocks: 11 total; 8 semantic; 3 fallback; 0 omitted
- Unresolved: **0 blocking, 3 warning**
- Warning queue: one flattened table requiring visual review and two extracted images
  requiring Owner-authored alt text/visual review
- Assets: **2** content-addressed JPEG files, 108,472 bytes total
- Chemistry, formulas, table fidelity, image meaning/alt text, mobile and print:
  **UNVERIFIED**, intentionally left for Claude presentation work and Owner QA.

## Decisions and assumptions

- Used source `T24-S01` because it is the sole Topic 24 source and is explicitly
  classified `primary_candidate`, `part_i_present`, `import_part_i` in the frozen
  source manifest. No ambiguous source choice was made.
- Used slug `phan-bon-hoa-hoc` and title `Phân bón hóa học`, mechanically derived
  from the unambiguous source title. They remain subject to normal Owner content QA.
- Did not infer chemical meaning. Extracted text is source text; uncertain tables and
  images remain visible and machine-readable in the failure/remediation records.
- Kept the historical script and manifest filenames to avoid an unrelated rename.
- The test generalizations are required consequences of the approved mixed-status
  manifest contract; they do not change release eligibility. PDF/library consumers
  still filter to `in_review`.

## Files and migrations changed

- `scripts/import-docx/pilot_import.py` — explicit/incremental importer, safe merge,
  draft QA/remediation output.
- `scripts/import-docx/prototype.py` — authorize manifest-backed `import_part_i`
  sources while retaining provenance validation.
- `scripts/import-docx/README.md` — incremental CLI and safety documentation.
- `content/topics.ts` — register Topic 24.
- `content/pilot-staging-manifest.json` — append the T24 draft and its two assets;
  existing pilot lesson objects are unchanged.
- `content/topics/chuyen-de-24/phan-bon-hoa-hoc.mdx` — canonical draft, Part I only.
- `content/qa/import-reports/phan-bon-hoa-hoc.failure.json` — machine-readable report.
- `content/qa/pending/phan-bon-hoa-hoc.json` — unsigned pending QA record.
- `content/qa/pending/phan-bon-hoa-hoc.remediation-queue.json` — three pending warning
  items with no Owner decision.
- `public/staging-assets/lessons/95/955c50af88187216dafd2353c74b540e510403a0c970d8f247cc0d2e01e25dce.jpeg`
  — extracted content-addressed source image.
- `public/staging-assets/lessons/d4/d430885438c33c62a0f3bbe2cf7e7969af17747d2da2be3622becd4639456453.jpeg`
  — extracted content-addressed source image.
- `tests/content/incremental-import.test.ts` — P6-B1.1 regressions.
- `tests/content/pilot-content-regression.test.ts` — contract-wide source-ID pattern.
- `tests/content/p5-release-pipeline.test.ts` — assert the `in_review` release subset,
  not every mixed-status manifest entry.
- `src/features/content/pilot-manifest.test.ts` — include and verify T24 draft.
- `docs/handoffs/P6/P6-B1.1-copilot.md` — this handoff.
- Migrations: none.

No ADR, contract, package, lockfile, workflow, auth, database or production file was
changed.

## Verification

- Explicit T24 importer rerun: passed; returned `{"result": "unchanged"}`.
- P6-B1.1 drift regression: passed; manual MDX drift was refused and all protected
  MDX/QA/report/queue/manifest/pilot hashes stayed unchanged after the refused run.
- Baseline comparison against `07e73d0`:
  - T06/T08 MDX, QA, failure and remediation files: byte-identical (`git diff
    --exit-code` returned 0).
  - T06/T08 manifest lesson objects: deep-equal before/after.
  - Existing pilot assets: byte-identical and not deleted.
- `npm run content:validate`: passed with `{"valid": true, "errors": []}`.
- Focused importer/content/PDF/library tests: passed.
- Full `npm run verify`: passed outside the filesystem sandbox after a clean offline
  `npm ci`: format, lint, typecheck, **41 test files / 169 tests**, and Next.js
  production build (13 static pages generated) all passed.
- `npm run pdf:dry-run`: passed; generated exactly the two `in_review` pilot PDFs
  (`dong-hoa-hoc` 232,170 bytes; `dung-dich-va-can-bang-hoa-hoc` 551,088 bytes).
  T24 `draft` was skipped.
- `git diff --check`: passed.

Initial verification attempts are not hidden: a temporary `node_modules` symlink in
the isolated worktree made Turbopack reject a dependency path outside its filesystem
root. Replacing it with local dependencies exposed sandbox `EPERM`/process behavior.
A clean `npm ci --offline` and final `npm run verify` outside that sandbox passed; no
tracked dependency file changed.

GitHub CI Validate and Vercel preview: PASS after the integration owner committed
and opened PR #32; see “Post-handoff integration update”.

## Environment and external services

- No secret was read, printed or changed.
- No R2, Neon, OAuth, production, public-bucket or other cloud operation occurred.
- No live PDF upload occurred; PDF validation was local dry-run generation only.
- No lesson was changed to `published`.

## Remaining work, known issues and risks

- P6-B1.2 / Claude must review mechanical MDX/component/mobile/print presentation for
  Topic 24 without making chemistry decisions. The three warnings must remain visible
  until dispositioned.
- Project Owner must verify Part I scope, chemistry, formula typography, table
  fidelity, image meaning/alt text, mobile and print before signing QA or advancing
  the lesson to `in_review`.
- T24 contains zero converter-blocking items, but this does not imply chemical or
  presentation approval; all seven QA checks remain false.
- P6-B1.3 remains available for additional presentation/E2E patterns discovered by
  Claude/Owner. This task only added regressions necessary for the new importer and
  mixed-status content shape.

## Plan deviations

- None. The work imports only Topic 24 and stays within the P6-B1.1 boundaries.

## Required inputs for the next task/phase

- Project owner/integration owner review and merge decision for the CI/Vercel-green
  PR #32.
- P6-B1.2 must use the canonical T24 MDX, failure report, QA record and remediation
  queue from this task, preserve `status: draft`, and leave QA decisions to the Owner.
- No further content topic should be imported until T24 converter and QA metrics have
  been reviewed under the P6 coordination gate.
