# Handoff P6-B1.2 — Claude Code

## Status

Complete (PR opened, not merged — review/merge is the project owner's decision).

## Inputs read

- `AGENTS.md`
- `KE_HOACH_XUYENLAB_CHEMISTRY.md` (Phase 6/7 sections)
- `docs/contracts/content.md`
- `docs/contracts/handoff.md`
- `docs/handoffs/P6/COORDINATION.md`
- `docs/handoffs/P6/P6-B1.0-claude.md`
- `docs/handoffs/P6/P6-B1.1-copilot.md`
- Topic 24 canonical MDX, failure report, pending QA record, remediation queue,
  both extracted image assets
- `src/components/pilot-reader/PilotLessonShell.tsx`,
  `src/components/mdx/ChemFigure/ChemFigure.tsx`,
  `src/components/mdx/DataTable/DataTable.tsx` (+ their CSS modules)
- `src/app/fixtures/pilot/chuyen-de-06/dong-hoa-hoc/*` and
  `chuyen-de-08/dung-dich-va-can-bang-hoa-hoc/*` (the established fixture-page
  pattern)
- `tests/e2e/pilot-staging.spec.ts`
- The task instruction (chat-provided, 2026-08-14), including the frozen
  "current Topic 24 facts" and hard exclusions

## Completed

- **Found and fixed a real broken route**, not just a polish item: P6-B1.1
  registered Topic 24 in the shared pilot manifest, so
  `listPilotLessonManifests()` (consumed by `/fixtures/pilot`'s index page)
  started returning three lessons and linking to
  `/fixtures/pilot/chuyen-de-24/phan-bon-hoa-hoc` — but no `page.tsx` existed
  for that route. Verified this with `npm run build` before my fix (route
  absent from the route table) and after (route present, `200` on request).
  Created `src/app/fixtures/pilot/chuyen-de-24/phan-bon-hoa-hoc/{page.tsx,body.mdx}`,
  byte-for-byte mirroring the existing T06/T08 pattern: `body.mdx` is
  `tail -n +17` of the canonical MDX (frontmatter stripped, verified identical
  via `diff`), `page.tsx` wires the same `PilotLessonShell` with Topic
  24's title/summary/manifest lookup. No chemistry content was written; every
  word is copied verbatim from the canonical MDX P6-B1.1 already produced.
- **Fixed stale, now-inaccurate copy** on `src/app/fixtures/pilot/page.tsx`:
  the heading ("Pilot Chuyên đề 6 & 8"), banner ("staging P4") and intro text
  ("Hai bài học nháp từ importer P4.1" — "two draft lessons from the P4.1
  importer") hardcoded assumptions that no longer hold now that a third,
  differently-sourced lesson exists. Generalized the copy so it doesn't need
  editing again for future batches, and added a per-lesson status badge
  (`draft` | `in_review`) to each card, since the manifest can now hold mixed
  lifecycle stages and the index previously had no way to show that.
- **Verified ChemFigure/DataTable need no changes for Topic 24.** Built the app,
  started a local production server, and used Playwright to capture and
  visually inspect desktop, mobile (390px) and print-emulated screenshots of
  the new page. The table's mobile behavior (`min-width: 400px` inside an
  `overflow-x: auto` region, so narrow viewports show a horizontally
  scrollable slice rather than wrapped/clipped text) initially looked like a
  bug; captured the same comparison against Topic 6's table on mobile and
  confirmed byte-for-byte identical CSS produces the same behavior there —
  this is existing, intentional P4 design, not something Topic 24 introduced
  or something in scope to change. Print media correctly expands the table to
  full width (`overflow-x: visible` in the component's `@media print` rule).
  Both extracted images render at legible size on desktop/mobile without
  overflow.
- **Confirmed the private production route
  (`src/app/(private)/chuyen-de/[topic]/[lesson]/page.tsx`) is unaffected**:
  it 404s for anything except a single hardcoded
  `STAGING_TOPIC_SLUG`/`STAGING_LESSON_SLUG` fixture constant and never reads
  `content/topics.ts` or the pilot manifest. Topic 24's registration has no
  effect on it.
- **Extended the existing pilot-staging Playwright spec**
  (`tests/e2e/pilot-staging.spec.ts`) to include Topic 24 in its lesson table,
  rather than writing a parallel spec — every one of its six desktop
  assertions (route loads clean, TOC matches every `h2`, every table sits in
  an `overflow-x: auto` wrapper, blocking Callouts match the remediation
  queue exactly, print media hides staging chrome, landmark/heading/alt-text/
  keyboard-focus checks) and its one mobile assertion (no content wider than
  a 375px viewport outside a table scroll region) are lesson-agnostic and
  already iterate a `lessons` array — Topic 24 slots in as a third entry with
  no per-lesson special-casing needed. Renamed the "loads both lessons..."
  test to "loads every lesson..." since it's no longer exactly two. All 7
  tests pass with Topic 24 included, which is also the automated proof behind
  the manual screenshot inspection above (mobile no-overflow, print-visible
  content, valid heading hierarchy, non-empty image alt attributes).
- Added a matching `page.test.tsx` for the new route (mirroring T06's
  MDX-import-sentinel pattern) and extended the pilot-index `page.test.tsx`
  with a regression asserting the Topic 24 link resolves to a real href, and
  that the index shows exactly two `in_review` and one `draft` status badge.

## Disposition of the three Topic 24 warnings

All three remain **unresolved and fully visible**, with no field written into
any `ownerDecision` block — this was a deliberate decision, not an oversight:

- **`T24-S01:t6971`** (table, `DOCX_TABLE_REQUIRES_VISUAL_REVIEW`): the
  flattened `DataTable` is rendered exactly as P6-B1.1 produced it. Not
  touched — visual/data fidelity review is a chemistry/content judgment, not
  a mechanical one.
- **`T24-S01:i8191`** and **`T24-S01:i0305`** (both
  `EXTRACTED_IMAGE_REQUIRES_REVIEW`): both `ChemFigure` elements are rendered
  exactly as produced, including the existing placeholder alt text ("Hình
  trích từ DOCX; cần chủ dự án bổ sung mô tả"). I viewed both source images —
  they are flowchart-style diagrams containing specific chemical formulas and
  category text — and considered writing a transcribed/descriptive alt text
  as a "mechanical, source-evidenced" fix. Decided against it: the
  remediation-queue schema names this field `ownerDecision.altText` for a
  reason, and every prior blocking/warning item in T06/T08 that needed a
  human judgment call (formula transcription, image fallback acceptance) was
  left for the project owner or explicitly authorized before any agent wrote
  into it. Writing chemistry-diagram alt text crosses from "mechanical
  presentation" into "asserting what the image chemically means," which the
  task's hard exclusions forbid me from inferring.
- `content/qa/pending/phan-bon-hoa-hoc.json` and `.remediation-queue.json`
  were not opened for writing at all (only read) — `approvedForPublish` stays
  `false`, no `publishWaiver`, `reviewer`/`reviewedAt` stay `null`, all seven
  checks stay `false`, exactly as instructed.

## Decisions and assumptions

- Treated the missing fixture route as in-scope "component gap" work (Allowed
  item 2: "Fix mechanical MDX/component usage, markup, layout and
  accessibility") rather than an exclusion, since it's a structural bug (a
  dead link introduced by P6-B1.1's manifest registration), not a content
  decision.
- Treated the pilot-index copy fix the same way: outdated hardcoded lesson
  count/phase references are a mechanical presentation defect, not a content
  judgment.
- Did not modify `ChemFigure`/`DataTable`/their CSS modules — verified via
  direct visual comparison against Topic 6 that their current behavior is
  correct, shared, and pre-existing; changing shared component behavior
  without a real defect would be an unrequested, unscoped change.
- Did not touch `content/topics/chuyen-de-24/phan-bon-hoa-hoc.mdx`,
  `content/qa/pending/phan-bon-hoa-hoc.json`,
  `content/qa/pending/phan-bon-hoa-hoc.remediation-queue.json`, or either
  extracted image asset. The fixture `body.mdx` copy is intentionally
  auto-derived (documented in its own header comment) rather than a second
  source of truth.
- Extended the existing `pilot-staging.spec.ts` in place instead of writing a
  new spec file, since every assertion in it already generalizes over a
  `lessons` array — duplicating it for Topic 24 would have been unnecessary
  and easy to let drift.

## Files and migrations changed

- `src/app/fixtures/pilot/chuyen-de-24/phan-bon-hoa-hoc/page.tsx` (new)
- `src/app/fixtures/pilot/chuyen-de-24/phan-bon-hoa-hoc/body.mdx` (new,
  auto-derived copy)
- `src/app/fixtures/pilot/chuyen-de-24/phan-bon-hoa-hoc/page.test.tsx` (new)
- `src/app/fixtures/pilot/page.tsx` — generalized copy, per-lesson status
  badge
- `src/app/fixtures/pilot/page.module.css` — `.status` class for the badge
- `src/app/fixtures/pilot/page.test.tsx` — Topic 24 link regression, status
  badge regression
- `tests/e2e/pilot-staging.spec.ts` — added Topic 24 to the shared lesson
  table; renamed one test title
- `docs/handoffs/P6/P6-B1.2-claude.md` — this handoff
- Migrations: none.
- **Not touched:** any file under `content/topics/chuyen-de-06`,
  `content/topics/chuyen-de-08`, `content/topics/chuyen-de-24`,
  `content/qa/**`, `content/pilot-staging-manifest.json`,
  `public/staging-assets/**`, `scripts/**`, or any lifecycle/importer/
  validator logic.

## Verification

- `npm ci` in the dedicated worktree, then:
- `python3 scripts/validate-content/validate.py --json`: passed
  (`{"valid": true, "errors": []}`).
- `npm run verify` (format:check, lint, typecheck, test, build): passed — 42
  test files / 170 tests, production build succeeded with 14 routes,
  including the new `/fixtures/pilot/chuyen-de-24/phan-bon-hoa-hoc` static
  route.
- `CI=1 npm run test:e2e -- tests/e2e/pilot-staging.spec.ts --reporter=line`:
  **7/7 passed**, now covering all three pilot lessons (desktop route/TOC/
  table-wrapper/blocking-callout/print/a11y checks, plus the mobile
  no-overflow check).
- **Pilot immutability, proven by diff against the exact baseline
  `20e2fd557bbc0a51e3dc3547fe3dab066df1b9c1`:**
  `git diff 20e2fd5 -- content/topics/chuyen-de-06 content/topics/chuyen-de-08
  content/qa/pending/dong-hoa-hoc.json
  content/qa/pending/dung-dich-va-can-bang-hoa-hoc.json
  content/qa/import-reports/dong-hoa-hoc.failure.json
  content/qa/import-reports/dung-dich-va-can-bang-hoa-hoc.failure.json
  content/pilot-staging-manifest.json public/staging-assets
  src/app/fixtures/pilot/chuyen-de-06 src/app/fixtures/pilot/chuyen-de-08`
  returned **empty** (exit 0, zero files changed).
- Full diff against baseline (`git diff 20e2fd5 --stat` +
  `git status --porcelain`) touches exactly 4 existing files and 1 new
  directory — matching the "Files and migrations changed" list above with no
  surprises.
- Manual visual verification: built the app, ran a local production server,
  and used Playwright to capture desktop/mobile/print screenshots of the
  index page and the new Topic 24 page (viewed directly, not just asserted);
  cross-checked the mobile table-scroll appearance against Topic 6's
  identical existing behavior before concluding it wasn't a defect.

## Environment and external services

- No secret was read, printed or changed.
- No R2, Neon, OAuth, production, public-bucket or other cloud operation
  occurred.
- No lesson `status`, QA record, or `approvedForPublish`/`publishWaiver` value
  was changed. Topic 24 remains `draft`, unsigned, exactly as received.
- Local scratch Playwright screenshot scripts used for manual visual
  inspection were deleted before committing; not part of this PR.

## Remaining work, known issues and risks

- The three Topic 24 warnings remain exactly as P6-B1.1 left them — visible,
  traceable, undecided. Project Owner QA (chemistry, table/image fidelity,
  final alt text authorship) is still required before `in_review`.
- `docs/qa/p4-remediation-queue*.md`-style human-readable remediation docs
  were not created for Topic 24 (P6-B1.1's `remediation-queue.json` already
  covers this in machine-readable form); flagging in case Owner QA expects a
  parallel Markdown document matching the P4 pattern — did not add one
  unprompted since it wasn't requested and would be speculative scope.
- No further content topic should be imported until Topic 24 converter/QA
  metrics are reviewed under the P6 coordination gate (per
  `P6-B1.1-copilot.md`), unchanged by this task.

## Plan deviations

- None. This task stayed within Claude's default UI/MDX/presentation
  ownership per `AGENTS.md`, and within every allowed/excluded boundary in
  the task instruction.

## Required inputs for the next task/phase

- Project owner review and merge decision for this PR.
- Project Owner QA of Topic 24 (chemistry correctness, table/image fidelity,
  final alt text) — the natural next step before this lesson can move to
  `in_review`. The two unauthored `ownerDecision.altText` fields and the
  table visual-review item are exactly what that QA pass needs to disposition.
- P6-B1.3 (per `COORDINATION.md`) remains available for any additional
  presentation/E2E patterns discovered during that Owner QA pass.
