# Handoff P6-B1.4 — Claude Code

## Status

Complete. Records the Project Owner's Topic 24 Owner QA decision at its
source of truth and promotes Topic 24 from `draft` to `in_review`. This is
the first task in the P6 phase to change canonical `content/` data since
P6-B1.1 imported Topic 24 as `draft`.

## Ownership / authorization

- **Owner decision:** Thầy Xuyên, recorded 2026-08-14. Thầy Xuyên reviewed
  Topic 24 Part I directly (not delegated review) and made three concrete
  dispositions (see "Decisions and assumptions" below), verbatim as
  instructed.
- **Implementer:** Claude Code, explicitly delegated by the Project Owner to
  record this decision in the repository.
- **Reviewer:** Codex integration owner, before merge.
- This delegation is scoped to recording an already-made Owner decision
  faithfully — it is not a delegation of chemistry judgment, QA discretion,
  or publication authority. No content/QA decision in this PR reflects
  Claude's own judgment about the chemistry or the images/table; every
  disposition, note, and value below was specified by the task instruction
  verbatim or derived mechanically (hashes, cross-references).

## Inputs read

- `AGENTS.md`
- `KE_HOACH_XUYENLAB_CHEMISTRY.md` (P6/P7 sections)
- `docs/contracts/content.md` (`LessonQaRecord`, "Remediation queue", "Staging
  manifest")
- `docs/contracts/handoff.md`
- `docs/handoffs/P6/COORDINATION.md`
- `docs/handoffs/P6/P6-B1.3P-claude.md` (operational-acceptance schema/validator)
- `docs/handoffs/P6/P6-B1.3U-claude.md` (presentation layer that now renders
  this real data)
- All earlier Topic 24 P6 handoffs: `P6-B1.0-claude.md`, `P6-B1.1-copilot.md`,
  `P6-B1.2-claude.md`, `P6-B1.3-copilot.md`
- `content/topics/chuyen-de-24/phan-bon-hoa-hoc.mdx`,
  `content/qa/pending/phan-bon-hoa-hoc.json`,
  `content/qa/pending/phan-bon-hoa-hoc.remediation-queue.json`,
  `content/qa/import-reports/phan-bon-hoa-hoc.failure.json`,
  `content/pilot-staging-manifest.json`

## Decisions and assumptions

Recorded exactly as instructed, not reinterpreted:

1. **Owner reviewed Topic 24 Part I on 2026-08-14 and approved all seven QA
   checks** (scope Part I, chemistry, formulas, tables, figures, mobile,
   print) — all set `true` in the QA record; `reviewer` set to the exact
   string `"Thầy Xuyên (Project Owner)"`; `reviewedAt` set to the date-only
   value `"2026-08-14"` (valid ISO 8601; no reliable time-of-day evidence
   exists, so none was invented — same reasoning already established for
   the P6.2 `publishWaiver.authorizedDate` correction).
2. **`T24-S01:t6971`** (table): Owner compared the flattened table with the
   Word source and confirmed the displayed values are accurate; accepted
   unchanged for teacher-led use; no content remediation was performed. →
   `status: "accepted-with-limitation"`,
   `remediationChoice: "owner-accepted-source-fidelity"`.
3. **`T24-S01:i8191`** and **`T24-S01:i0305`** (images): Owner visually
   verified both source images and accepts the current placeholder alt text
   unchanged (`"Hình trích từ DOCX; cần chủ dự án bổ sung mô tả"`); no
   meaningful semantic alt text or accessibility improvement was authored or
   applied; the limitation remains visible and source-traceable. →
   `status: "accepted-with-limitation"`,
   `remediationChoice: "owner-accepted-visible-fallback"` for both.

For all three: `ownerDecision.altText`/`caption`/`reviewedLatex` stay `null`
(no remediation payload was authored); `issueId`, `sourceId`, `topic`,
`lessonSlug`, `sourceLocator`, `issueCode`, `kind`, `severity`, `message`,
`observedType`/`observedTypeEvidence`, and `previewPath` are byte-for-byte
unchanged from before this PR (verified against the untouched failure report
in `tests/content/topic24-owner-qa.test.ts`); each issue remains in the QA
record's `unresolved` array exactly as before; no `discussionPrompt` was
added to any item, since the Owner did not author a concrete discussion
question/objective (the task explicitly says not to invent one).

`approvedForPublish` stays `false` and no `publishWaiver` was added — Topic
24 is not one of the two P6.2-named exception lessons
(`P6_OWNER_APPROVED_PUBLISH_SLUGS` in `scripts/validate-content/validate.py`
still lists only `dong-hoa-hoc`/`dung-dich-va-can-bang-hoa-hoc`), and this
task does not add it to that allowlist.

MDX frontmatter `summary` and `keywords` were reworded (`"pilot-draft"` →
`"pilot-in-review"`; summary no longer claims "vẫn chờ Chủ dự án QA" now that
Owner QA has happened) since those specific fields would otherwise become
false the moment `status` changed — no other frontmatter field, and no
byte of the Chemistry body/table/`ChemFigure`/formulas, was touched
(verified in `tests/content/topic24-owner-qa.test.ts`, and `body.mdx` under
`src/app/fixtures/pilot/chuyen-de-24/phan-bon-hoa-hoc/` — a byte-identical
copy of the canonical body per its own header comment — needed no
regeneration, since only the frontmatter changed).

## Files and migrations changed

- `content/qa/pending/phan-bon-hoa-hoc.json` — all 7 `checks` → `true`;
  `reviewer`/`reviewedAt` signed; `reviewStatus` → `"in_review"`;
  `approvedForPublish` stays `false`; `unresolved`/`lessonVersion`/
  `sourceIds` unchanged.
- `content/qa/pending/phan-bon-hoa-hoc.remediation-queue.json` — the three
  Topic 24 entries updated per the exact dispositions above; every other
  field preserved.
- `content/topics/chuyen-de-24/phan-bon-hoa-hoc.mdx` — frontmatter `status:
  draft` → `in_review`; `summary`/`keywords` reworded (lifecycle-only);
  body/table/`ChemFigure`/formulas untouched.
- `content/pilot-staging-manifest.json` — Topic 24 lesson entry: `status` →
  `"in_review"`; `mdxSha256`/`qaSha256` refreshed to match the two changed
  files above; `blockingCount`/`warningCount`/provenance/`failureReportSha256`
  unchanged; `manifestVersion` unchanged (`"1.1.0"`); Topic 6/8 entries
  untouched (verified deep-equal to their known-committed shape in
  `tests/content/topic24-owner-qa.test.ts`).
- `tests/content/topic24-owner-qa.test.ts` — new, 17 tests: QA-record
  signing, disposition pairs, null remediation-payload fields, no
  `discussionPrompt`, cross-reference against the untouched failure report,
  lifecycle metadata, hash refresh, T06/T08 preservation.
- `tests/content/operational-acceptance.test.ts` — split the P6-B1.3P
  "legacy-only" characterization into a T06/T08-only assertion plus a new,
  separate assertion that Topic 24's three items are exactly the
  P6-B1.4-authorized `accepted-with-limitation` dispositions (the file no
  longer treats Topic 24 as "must still be legacy-only", since it now
  legitimately isn't).
- `src/app/fixtures/pilot/page.test.tsx` — the pilot-index intro test no
  longer requires a draft lesson to currently exist (Topic 24 was the last
  one); it still proves the manifest is exactly draft/in_review (never
  published) and that the intro's generic wording covers both.
- `src/app/fixtures/pilot/chuyen-de-24/phan-bon-hoa-hoc/page.test.tsx` — fixed
  an ambiguous `getByText("T24-S01")` match (now appears 4 times, not 1);
  added 3 tests: in_review banner, all three real accepted items present,
  no misleading fixed/verified/accessibility-improved copy.
- `src/features/content/p5-library/library.test.ts` — added a real-data test:
  Topic 24 now appears with `acceptedLimitationsCount: 3`.
- `src/features/content/p5-library/LibraryFixture.test.tsx` — added a
  real-data test: Topic 24's card shows "Giới hạn được lưu theo nguồn" → `3`.
- `tests/e2e/pilot-staging.spec.ts` — added a Playwright test proving all
  three real accepted-limitation dispositions render with no misleading
  copy; updated a stale comment.
- `tests/e2e/p5-library-search.spec.ts` — added a Playwright test proving
  Topic 24's real card shows count 3; updated a stale comment.
- `docs/handoffs/P6/P6-B1.4-claude.md` — this handoff.
- Migrations: none.

## Verification

- `npm run content:validate` — passed, `{"valid": true, "errors": []}`.
- Focused:
  `npx vitest run tests/content/topic24-owner-qa.test.ts tests/content/operational-acceptance.test.ts src/app/fixtures/pilot src/features/content/p5-library`
  — passed (17 + 28 + 8 + 16 tests across the affected files).
- Full unit suite: `npx vitest run` — passed, **48 test files / 266 tests**.
- Focused Playwright:
  `CI=1 npx playwright test tests/e2e/pilot-staging.spec.ts tests/e2e/p5-library-search.spec.ts --reporter=line`
  — passed, **21/21** (2 new P6-B1.4 tests plus every pre-existing
  regression, all lifecycle-derived assertions naturally picking up Topic
  24's real promotion without further test changes).
- `npm run verify` (format:check + lint + typecheck + `vitest run` +
  `next build`) — passed: 48 test files / 266 tests, 0 lint/type/format
  errors, build succeeded (same single pre-existing, already-documented
  Turbopack tracing warning as every prior P6 phase).
- `npm run pdf:dry-run` — passed. Eligible set after promotion is exactly
  `dong-hoa-hoc`, `dung-dich-va-can-bang-hoa-hoc`, `phan-bon-hoa-hoc` (T06,
  T08, T24) — confirmed by the actual generated PDF filenames/hashes in the
  command output, matching the expected set exactly.
- `git diff --check` — passed, no whitespace/conflict-marker issues.
- `git status --porcelain` against `origin/main` — contains only the files
  listed above; no importer, validator, contract, workflow, or lockfile
  change.
- T06/T08 immutability, confirmed byte-identical:
  `git diff --exit-code origin/main -- content/topics/chuyen-de-06 content/topics/chuyen-de-08 content/qa/pending/dong-hoa-hoc.json content/qa/pending/dung-dich-va-can-bang-hoa-hoc.json content/qa/pending/dong-hoa-hoc.remediation-queue.json content/qa/pending/dung-dich-va-can-bang-hoa-hoc.remediation-queue.json content/qa/import-reports/dong-hoa-hoc.failure.json content/qa/import-reports/dung-dich-va-can-bang-hoa-hoc.failure.json public/staging-assets`
  — exit 0, byte-identical; plus the two manifest lesson entries verified
  deep-equal in `tests/content/topic24-owner-qa.test.ts`.

## Environment and external services

- No secret was read, printed, or changed.
- No R2, Neon, OAuth, production, public bucket, or deployment action
  occurred. No workflow, auth, or account/login change.

## Remaining work, known issues and risks

- **This does not authorize publication, production, R2, public access,
  Auth/Neon, or P7.** Topic 24 is `in_review`, not `published`;
  `approvedForPublish` stays `false`; no `publishWaiver` exists for it. The
  normal publication rules (a future, separate, explicit Owner
  `approvedForPublish` decision, itself still gated on `published` requiring
  the project owner) remain entirely untouched.
- Topic 2 and every other lesson outside Topic 24 were not touched.
- No contract, validator, importer, PDF/R2, workflow, auth, or production
  file was changed — this PR is data-only against the already-existing
  P6-B1.3P schema and validator.
- Both images retain the original generic placeholder alt text; a future,
  separate task would be required to author real semantic alt text, which
  this task explicitly does not do.

## Plan deviations

- None. Executed exactly the scope and hard exclusions in the task
  instruction.

## Required inputs for the next task/phase

- Codex review of this PR before merge.
- After merge, per the Owner's stated sequence: any further P6 phase (e.g.
  P6-B1.5 integration/metrics per `docs/handoffs/P6/COORDINATION.md`) reads
  Topic 24 as `in_review` with three real Owner-accepted limitations, no
  `discussionPrompt` yet, and `approvedForPublish: false`.
