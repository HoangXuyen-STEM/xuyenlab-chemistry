# Handoff P6-B2.1 — Claude Code

## Status

Complete. **No presentation code change was required.** Verifies Topic 2's
fixture route on a production/`main`-baseline build (Topic 2 itself is
already live on `main` via P6-B2.0) and updates
`docs/handoffs/P6/COORDINATION.md` to reflect that P6-B1.5 and P6-B2.0 are
complete on `main`. This task's own PR (#43) is **not yet merged** —
everything in this handoff describes work done and evidence gathered, not
a state already integrated into `main`.

## Ownership / authorization

- Presentation/MDX-component work is Claude-owned by plan (the same role as
  P6-B1.2). This task is **not** a Copilot-quota deviation.
- This task is a plan **extension**, not a plan-named step: the plan names
  P6-B2.0 (preflight/import); this follow-on presentation-verification and
  coordination-update step was explicitly authorized by the Project Owner
  after P6-B2.0 (PR #41) merged, on 2026-08-15.

## Inputs read

- `docs/handoffs/P6/P6-B2.0-topic2-preflight-claude.md`,
  `docs/handoffs/P6/P6-B2.0-import-claude.md` (this task's prerequisite,
  merged as PR #41, commit `f4f4f5322936a1587330a4d962de360e14763407`)
- `docs/handoffs/P6/COORDINATION.md` (updated by this task)
- `src/app/fixtures/pilot/chuyen-de-02/bang-tuan-hoan/page.tsx`,
  `body.mdx` (the route under audit, unchanged by this task)

## Evidence

Confirmed `f4f4f53` is merged to `origin/main` (`gh pr view 41` →
`state: MERGED`, `mergeCommit: f4f4f53...`) and GitHub Validate + Vercel
Production both reported `SUCCESS` on that commit (per this task's own
instruction, deployment id `5919055939`).

**No public production URL for this project is documented anywhere in the
repository** (`docs/`, ADRs, handoffs), and none was supplied. Per this
task's own fallback instruction ("If production is auth-gated, use local
production build of main instead and state that clearly"), and per the
standing instruction to never guess or construct a URL, this audit uses a
**local production build of `main` at `f4f4f53`** (`npm run build` +
`npm start`) instead of a live deployment. This is a materially equivalent
artifact to what Vercel actually deployed — same commit, same `next build`
output, same static route.

All checks below ran against `http://127.0.0.1:3211` (a locally started
`next start` production server), via a throwaway Playwright script (not
committed; deleted after use) using `chromium`, in three passes: desktop
(1280×900), mobile (390×844, `isMobile: true`), and `print` media emulation.

### 1) Production/main evidence

- `GET /fixtures/pilot` → **200**. The Topic 2 card is present, text:
  `"Bảng tuần hoàn · draft bang-tuan-hoan Nguồn T02-S01 · 1 mục chặn · 2
  cảnh báo"` — draft status and exactly 1 blocking / 2 warning shown,
  matching the manifest exactly.
- `GET /fixtures/pilot/chuyen-de-02/bang-tuan-hoan` → **200**. Zero browser
  console errors on load.
- Banner text: `"[BẢN NHÁP PILOT] — Xem trước staging, không cần đăng nhập —
  chưa xuất bản"` — visible, correctly derived from `manifest.status ===
  "draft"`.
- Blocking Callout: exactly 1, `aria-label="Hình vẽ Word cần biên tập"`, text
  `"Chưa chuyển hình vẽ T02-S01:d1402 (số 1)."` — the drawing's stable issue
  ID visible and traceable.
- Image: 1 `<figure><img>`, `src="/staging-assets/lessons/76/76e8a93...png"`,
  `alt="Hình trích từ DOCX; cần chủ dự án bổ sung mô tả"` — visible.
- Table: 1 `<table>`, visible, inside its overflow wrapper.

### 2) Presentation audit (desktop / mobile / print)

- **Desktop** (screenshot captured): banner, staging notice, breadcrumb,
  title, provenance line, TOC (3 entries matching the 3 `##` headings),
  body content, the flowchart image with its caption, the flattened table,
  and the blocking Callout all render correctly with no visible layout
  defect.
- **Mobile ~390px** (screenshot captured):
  `document.documentElement.scrollWidth === window.innerWidth` → `true`
  (no horizontal overflow); banner visible; content reflows correctly;
  image and table both fit within the viewport.
- **Print emulation** (screenshot + PDF captured): decorative banner
  `display: none`; the staging limitation notice (`role="note"`, "Lưu ý bản
  đang duyệt") remains visible; `<h1>` remains visible
  (`offsetParent !== null`); the print button is hidden. Same behavior
  already proven for T06/T08/T24 by the existing `pilot-staging.spec.ts`
  print-media tests, now visually confirmed for T02 too.

### Verdict: no presentation code change required

Every check above matches the existing, already-tested T06/T08/T24 pattern
exactly, with no defect specific to Topic 2's content (its 1 native-drawing
blocking item, 1 image warning, 1 table warning). This is the same outcome
already proven mechanically by PR #41's own unit/E2E coverage
(`src/app/fixtures/pilot/chuyen-de-02/bang-tuan-hoan/page.test.tsx`, and the
Topic-2-specific assertions added to `tests/e2e/pilot-staging.spec.ts`) —
this task's production-baseline audit independently reconfirms the same
result on the actual merged `main` build rather than the PR branch.

## Files and migrations changed

- `docs/handoffs/P6/COORDINATION.md` — records P6-B1.5 and P6-B2.0 as
  complete on `main`; replaces the "Topic 2 not ready to import" gate with
  "Owner QA for Topic 2" as the current gate; adds `P6-B2.0`/`P6-B2.1`
  sections following the file's existing per-task format.
- `docs/handoffs/P6/P6-B2.1-topic2-presentation-claude.md` — this handoff.
- Migrations: none. No `content/`, MDX, QA, manifest, source DOCX, importer,
  validator, package, lockfile, auth, PDF/R2 or workflow file was touched.
  `src/app/fixtures/pilot/chuyen-de-02/` was read but not modified — the
  audit found no defect requiring a fix.

## Verification

- `npm run build` on `main` `f4f4f53`: passed, 15 static pages, including
  `/fixtures/pilot/chuyen-de-02/bang-tuan-hoan` — same known, already-
  documented Turbopack tracing warning as every prior P6 phase.
- Local production server (`npm start`) evidence: see "Evidence" above.
- `git status --porcelain` in the audit worktree: clean except this
  handoff and the `COORDINATION.md` update — no other file changed.

## Environment and external services

- No secret was read, printed or changed.
- No R2, Neon, OAuth, production deployment, or public bucket action
  occurred — the "production" evidence in this task is a local production
  *build*, not a live deployment or upload.

## Remaining work, known issues and risks

- **Owner-only gates, not authorized by this task**: Owner QA for Topic 2
  (chemistry, formulas, table fidelity, image meaning/alt text, mobile,
  print — the same seven checks P6-B1.4 satisfied for Topic 24); a
  disposition for each of the 3 unresolved items (image, table, drawing);
  any lifecycle promotion (`draft` → `in_review`); `approvedForPublish`;
  publication; P7.
- No documented public production URL exists for this project anywhere in
  the repository; a future task needing live-deployment verification should
  either have the Owner supply one explicitly or continue using a local
  production build, consistent with the standing instruction against
  guessing URLs.

## Plan deviations

- None in the "reassigned due to quota" sense — presentation work is
  Claude's normal plan role. This task itself is a plan **extension**: the
  written plan names P6-B2.0 (preflight/import) but not a P6-B2.1 follow-on;
  the Project Owner explicitly authorized this presentation-verification and
  coordination-update step after P6-B2.0 merged.

## Required inputs for the next task/phase

- `docs/handoffs/P6/COORDINATION.md` (updated) and this handoff.
- A future Owner QA task for Topic 2 (parallel to P6-B1.4), gated entirely
  on the Project Owner, not authorized here.
