# Handoff P6-B2.13 — Claude Code

## Status

Complete. Applies the Project Owner's approved visual diagram
replacement to `T06-S01:d6703`, the sole remaining blocking drawing
item in Chuyên đề 06 (`dong-hoa-hoc`). Uses the established
`applied`/`reviewed-image-fallback` (`kind: "drawing"`) pairing
already legalized by the P6-B2.4B policy amendment and proven on
`T02-S01:d1402`. This task's own PR is **not yet merged**.

## Ownership / authorization

- **Owner decision:** approved replacing the placeholder Callout with a
  clean diagram of the cyclopropane → propene isomerization, per the
  task instruction's exact meaning, alt text, and caption.
- Claude Code authored the actual diagram asset (an SVG, rasterized to
  PNG) since no pre-existing candidate asset was supplied — unlike
  Topic 2's `d1402`, where a separate PR (#46) had already produced and
  gotten Owner sign-off on the candidate image before the applying PR
  (#48). This task's instruction authorized generating the asset
  directly in the same step; no separate "candidate-only" round was
  requested.
- Integration review before merge; not merged by this task.

## Prerequisites re-verified before any edit

- `main` at `1c96d7722ac0f3658cd37bbf7f8ed570869a52cf` (PR #56 merged —
  note the task's stated baseline SHA `6726a9f...` was the PR branch's
  pre-merge head; the real post-squash-merge SHA on `main` differs, the
  same recurring pattern as every prior PR in this chain).
- `T06-S01:d6703` independently confirmed real: `kind: "drawing"`,
  `severity: "blocking"`, `status: "pending-owner-review"`, and the
  **only** `pending-owner-review` item with `kind: "drawing"` — the
  other 3 remaining pending items are all `kind: "table"` /
  `severity: "warning"`, out of scope for this task.
- Confirmed the `applied` + `reviewed-image-fallback` (`kind:
"drawing"`) combination is still supported by
  `scripts/validate-content/validate.py`
  (`REMEDIATION_APPLIED_IMAGE_FALLBACK_KINDS = {"drawing"}`,
  `validate_applied_image_fallback`) — this was the P6-B2.4B policy
  amendment (PR #47), already merged; no contract or validator change
  needed here.
- Read `validate_applied_image_fallback` in full to derive the exact
  required shape before writing anything: `ownerDecision.reviewedLatex`
  must be `null`; `altText`/`caption` both required non-empty;
  `previewPath` required and must exactly string-match a
  `<ChemFigure src=... alt=... caption=... />` tag's own
  `src`/`alt`/`caption` attributes (via
  `find_chemfigure_attribute_pairs`, an order-insensitive regex over
  each ChemFigure tag); no Callout naming the issue id may remain.
- Read the real, merged `T02-S01:d1402` precedent (`content/topics/
chuyen-de-02/bang-tuan-hoan.mdx` and its remediation-queue entry) to
  confirm two conventions the task instruction's literal example paths
  didn't show explicitly:
  - **The asset path must carry a leading `/`** — both the MDX
    `src` and the queue's `previewPath` are
    `/staging-assets/lessons/<xx>/<sha256>.<ext>`, not
    `staging-assets/...` without the slash. Confirmed this is required,
    not just conventional: `scripts/validate-content/validate.py`'s
    `HASHED_ASSET` regex is anchored `^/staging-assets/lessons/...` —
    a path without the leading slash would silently skip the
    hash/existence check entirely rather than validating cleanly, and
    would also fail the `find_chemfigure_attribute_pairs` string-match
    against a `previewPath` that did carry the slash. Used the leading
    slash in both places, diverging from the task's literal example
    text (which omitted it) to match the actual validator contract and
    the real precedent.
  - Read the ChemFigure component (`src/components/mdx/ChemFigure/
ChemFigure.tsx`) to confirm `sourceId` is an untyped, optional,
    unvalidated prop (rendered only as `data-source-id`) — the T02
    precedent uses just the source document id (`"T02-S01"`), but since
    the task explicitly specified `sourceId="T06-S01:d6703"` and no
    validator or test constrains this field's exact value either way,
    followed the task's literal instruction here rather than the
    precedent's convention.

## Decisions and assumptions

- **Asset authored as an SVG then rasterized to PNG** (via Inkscape),
  matching the task's literal file-extension example
  (`<sha256>.png`) while using SVG as the authoring format for a crisp,
  reproducible vector diagram — the same tool used for T02's PR #46
  candidate (also SVG, though committed directly as `.svg` there; this
  task's own instruction named `.png` explicitly, so rasterized rather
  than committing the `.svg` directly).
- **Diagram content**: a labelled triangle (cyclopropane ring, `CH₂` at
  each vertex, captioned "Xyclopropan (C₃H₆)") → arrow labelled `t°`
  (matching the reaction condition already present in the adjacent
  `$$\text{Cyclopropan} \xrightarrow{t^\circ} \text{CH}_3\text{-CH=CH}_2$$`
  LaTeX, applied earlier in P6-B2.9's Batch A3) → a bent skeletal line
  for propene with `CH₃`/`CH`/`CH₂` vertex labels and a double bond
  mark, captioned "Propen (CH₃-CH=CH₂)". Cross-checked against the
  surrounding MDX text fragments left over from the original OOXML
  extraction ("CH2" immediately before the Callout, "H2CCH2"
  immediately after) — consistent with the drawing having depicted a
  3-vertex ring transforming to a chain, confirming the task's stated
  meaning rather than assuming it uncritically.
- **Asset registered in the manifest's `assets` array** (`sourceIds:
["T06-S01"]`), per the task's explicit instruction — this diverges
  from the `T02-S01:d1402` precedent, whose candidate SVG was **not**
  added to that array (that array otherwise contains only real,
  importer-extracted assets with genuine import provenance, not
  Owner-authored recreations). Checked before complying: no test
  asserts a fixed length or exact membership on the real committed
  manifest's `assets` array (the one `toHaveLength(19)` assertion in
  `tests/content/pilot-import.test.ts` runs against a temp import
  target, not this file), so adding the entry is harmless and was kept
  since the task explicitly asked for it.
- **Manifest edits done as single-field/single-entry `Edit`s, not a
  JSON re-dump** — same pitfall already found and avoided in P6-B2.7
  (a `json.dump`-based rewrite reformats unrelated compact arrays
  elsewhere in the file).
- `blockingCount`/`warningCount` stay `96`/`3` (frozen historical
  metric, same rule as every prior batch); QA `unresolved` stays
  exactly as-is — `d6703` remains listed at `severity: "blocking"`.
- Fixture `body.mdx` regenerated the same way as every prior T06 task:
  canonical MDX with its frontmatter block stripped.
- **`tests/content/remediation-queue.test.ts` needed a real logic
  change, not just a data update**, because this is the first `kind:
"drawing"` + `reviewed-image-fallback` item in either lesson this
  test file covers: the existing "every P4.5-processed entry" test
  unconditionally asserted `remediationChoice === "reviewed-latex-mdx"`
  for every non-`blocked` processed id, which is false for `d6703`
  (`"reviewed-image-fallback"`). Added a new
  `IMAGE_FALLBACK_APPLIED_ISSUE_IDS` set (currently just `d6703`) and a
  third branch asserting the correct shape (`reviewed-image-fallback`,
  `reviewedLatex: null`, non-empty `altText`/`caption`) instead of
  loosening the existing branch's assertions.

## Files and migrations changed

- `content/topics/chuyen-de-06/dong-hoa-hoc.mdx` — the `d6703` Callout
  replaced by a `<ChemFigure>` tag (src/alt/caption/sourceId as
  specified). No other text changed; the file now contains **zero**
  remaining `<Callout>` tags of any kind.
- `content/qa/pending/dong-hoa-hoc.remediation-queue.json` — `d6703`:
  `status` → `"applied"`, `remediationChoice` →
  `"reviewed-image-fallback"`, `previewPath` set,
  `ownerDecision` populated exactly as specified. All other fields and
  all other 98 items untouched.
- `content/pilot-staging-manifest.json` — `mdxSha256` for
  `chuyen-de-06/dong-hoa-hoc` refreshed to
  `290f201845b4ef601a2924c2481c265136660ce911c56ac5571388b04fee35fa`;
  new `assets` entry for the diagram PNG (`bytes: 18150`,
  `sourceIds: ["T06-S01"]`), inserted in the array's existing
  hash-sorted order. Nothing else in the file changed.
- `public/staging-assets/lessons/33/33f44212249802055b6ea0c0bfc8d605ab3b35aaa5205335230cc7cd84cd5d36.png`
  — new file, the diagram asset (680×300 PNG, content-addressed path;
  prefix `33` matches the sha256's first 2 hex chars, verified by the
  validator's `HASHED_ASSET` check).
- `src/app/fixtures/pilot/chuyen-de-06/dong-hoa-hoc/body.mdx` — synced
  to the canonical MDX body (frontmatter stripped), same one block
  changed.
- `tests/content/remediation-queue.test.ts` — added `d6703` to
  `PROCESSED_ISSUE_IDS`, added `IMAGE_FALLBACK_APPLIED_ISSUE_IDS` and
  its branch in the "every P4.5-processed entry" test, updated the doc
  comment.
- `tests/content/topic2-owner-qa.test.ts`,
  `tests/content/topic24-owner-qa.test.ts` — updated the hard-coded T06
  `mdxSha256` to the new real value (same recurring update as every
  prior T06 task).
- `docs/handoffs/P6/P6-B2.13-drawing-d6703-t06-claude.md` — this
  handoff.
- Migrations: none. **Not changed:** `docs/contracts/content.md`,
  `scripts/validate-content/validate.py` (the P6-B2.4B policy path
  already covers this case; no amendment needed), `content/qa/pending/
dong-hoa-hoc.json` (QA record itself), `content/qa/import-reports/
dong-hoa-hoc.failure.json`, any T02/T08/T24 content/QA/
  remediation-queue file, `package.json`, `package-lock.json`, any
  auth/R2/workflow file, `docs/handoffs/P6/COORDINATION.md`.

## Verification

- `npm run content:validate` — **PASS on the first attempt**,
  `{"valid": true, "errors": []}`, via the
  `reviewed-image-fallback`/`kind: "drawing"` validator path.
- Full unit suite (`npx vitest run`) — **PASS**, 51 test files / 312
  tests (3 initially failed on stale hard-coded T06 state / an
  incomplete branch in `remediation-queue.test.ts`'s own logic — fixed
  as described above, then re-ran clean).
- `npm run build` — **PASS**, no errors.
- Focused Playwright (`tests/e2e/pilot-staging.spec.ts`) — **PASS**,
  19/19, including "renders all blocking fallback warnings visibly with
  stable issue IDs" (T06 no longer contributes any blocking Callout to
  that assertion — only T08's still-blocked item does now).
- `git diff --name-status origin/main` — confirmed exactly 7 tracked
  files changed plus the one new asset file (`git status --porcelain`
  shows the asset as untracked `??` prior to `git add`): the MDX, the
  remediation queue, the manifest, the fixture, and the 2 test files
  with hard-coded hash updates plus `remediation-queue.test.ts`'s logic
  change. No T02/T08/T24 content, QA, or remediation-queue file
  changed.
- **Real visual verification, not just automated checks**: started a
  local production server (`npm start`, isolated port 4113) and used a
  scripted headless-browser screenshot of the actual rendered
  `<ChemFigure>` on `/fixtures/pilot/chuyen-de-06/dong-hoa-hoc` —
  confirms the diagram renders cleanly (triangle → arrow → bent chain,
  all labels legible, caption correct) rather than relying only on the
  standalone PNG file render. Server killed after the check; port 4113
  confirmed clear.

## Environment and external services

- No secret was read, printed, or changed.
- No R2, Neon, OAuth, production, public bucket, or deployment action
  occurred.

## Remaining work, known issues and risks

- **T06's remediation queue is now down to 3 pending items, all
  warning-severity tables** (`t2740`, `t3041`, `t6560`) — a
  table-review disposition (`accepted-with-limitation`/
  `owner-accepted-source-fidelity`, the P6-B1.3P vocabulary already
  proven for Topic 2/24's table items) would be the applicable path if
  the Owner wants them dispositioned. No blocking item remains
  unaddressed in T06.
- **`blockingCount: 96` in the manifest stays frozen** — historical
  import-time metric, unaffected, matching the rule already established
  for every lesson in this phase.
- **Not a publication or governance change.** `dong-hoa-hoc` keeps its
  existing `approvedForPublish: true` P6.2 `publishWaiver`
  (`unresolvedBlockingCount: 96`, unchanged — `unresolved` stays frozen
  by design). `status` stays `in_review`; nothing here authorizes
  production or public access.
- No further P6 work is authorized by this task.

## Plan deviations

- Diverged from the task's literal example paths (no leading `/`) to
  match the actual validator contract (`HASHED_ASSET` requires the
  leading slash) and the real `T02-S01:d1402` precedent — a correction,
  not a scope change.
- Diverged from the `T02-S01:d1402` precedent by adding the diagram to
  the manifest's `assets` array, per the task's explicit instruction;
  confirmed harmless first.
- Extended `remediation-queue.test.ts`'s own branching logic (a real
  code change, not just data), because no prior T06/T08 item exercised
  the `reviewed-image-fallback` branch of that particular test file
  before this task.
- No other deviation. Implemented exactly the single named item, no
  more, no less — did not touch the 3 remaining table warnings even
  though they sit in the same remediation queue.

## Required inputs for the next task/phase

- Integration review and Owner merge decision for this PR.
- After merge: T06 has zero remaining blocking items. Any future work
  on the 3 table warnings would need its own fresh Owner decision and
  scoped task, following the P6-B1.3P `accepted-with-limitation`
  pattern already used for Topic 2/24.
