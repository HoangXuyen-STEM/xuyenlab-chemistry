# Handoff P6-B2.5 — Claude Code

## Status

Complete. Records the Project Owner's explicit authorization of
`approvedForPublish: true` for Topic 2 (`bang-tuan-hoan`), the same
T06/T08-style (P6.2) per-lesson exception to the "no blocking issue
remains" rule, now naming a third lesson. **This is not `status:
"published"`, not production deployment, not public bucket access, and
not automatic publication** — those remain separate, later Owner/P7
decisions. This task's own PR (#49) is **not yet merged**.

## Ownership / authorization

- **Owner decision:** Thầy Xuyên, 2026-08-16. Chose this T06/T08-style
  approval path (option A) after explicitly rejecting "production now" —
  wants the governance state matched to T06/T08, not a P7 production
  decision.
- Claude Code implements the recording; this is not a chemistry, QA
  discretion, or new content decision — every disposition already exists
  (P6-B2.2, P6-B2.4B), this task only adds the publish-exception waiver on
  top of them.
- Integration review before merge; not merged by this task.

## Prerequisites re-verified before any edit

- `main` at `fbbb97b05eced6b72f894f8319db55fbb508b7b3` (matches this
  task's stated baseline; PR #48 confirmed merged via `gh pr view 48`).
- `T02-S01:d1402`: `status: "applied"`, `remediationChoice:
"reviewed-image-fallback"`, Callout absent from the MDX, the candidate
  `ChemFigure`'s `caption` still contains the literal `T02-S01:d1402`.
- QA `unresolved` still exactly: `T02-S01:i6022` (warning),
  `T02-S01:t7931` (warning), `T02-S01:d1402` (blocking).
- `approvedForPublish: false`, no `publishWaiver`, before this task's edit.
- `P6_OWNER_APPROVED_PUBLISH_SLUGS` was exactly
  `{"dong-hoa-hoc", "dung-dich-va-can-bang-hoa-hoc"}` before this task's
  edit.

## Decisions and assumptions

- **The historical P6.2 Amendments entries were left word-for-word
  unchanged.** This file's own established convention treats each dated
  Amendments entry as an immutable historical record of what happened at
  that time (confirmed by how the P6.2 audit-trail correction was added
  as a _new_ entry rather than editing the original P6.2 text). The
  phrases "exactly two"/"either lesson" inside those dated 2026-08-13
  entries remain accurate as historical statements about what P6.2
  itself did; they were not edited. A **new**, separately dated P6-B2.5
  entry was added instead, following the same pattern.
- **One current-state comment genuinely needed updating**: the Python
  comment directly above `P6_OWNER_APPROVED_PUBLISH_SLUGS` said "exactly
  these two ... lessons," which is a live descriptive comment (not a
  dated historical record) and would have become false the moment a
  third slug was added. Updated it to name the mechanism generically
  rather than hardcode a count that will keep drifting.
- **`bang-tuan-hoan` added to `PUBLISH_WAIVER_REQUIRED_ACKNOWLEDGED_BLOCKED_ITEMS`**
  mapping to `{"T02-S01:d1402"}`, the exact same discipline already used
  for `dung-dich-va-can-bang-hoa-hoc` → `{"T08-S01:e6352"}` — the waiver
  must name the specific blocked item it accepts, not just a count.
- **`publishWaiver` shape copied exactly from the T08 precedent**
  (`type`, `scope`, `doesNotAuthorize` set, `remediationDebtRetained`),
  with lesson-specific values: `authorizedDate: "2026-08-16"`,
  `unresolvedBlockingCount: 1` (matches the real current count — `d1402`
  is the only blocking item), `acknowledgedBlockedItems:
["T02-S01:d1402"]`.
- **QA `unresolved` array, `checks`, `reviewer`, `reviewedAt`,
  `reviewStatus` all left byte-unchanged** — this task only adds
  `approvedForPublish`/`publishWaiver`; it does not touch anything about
  the lesson's chemistry/QA review itself.
- **`d1402` stays in `unresolved` at `severity: "blocking"`** — the
  waiver's whole point is acknowledging that this specific historical QA
  severity remains, exactly as the P6.2 precedent does for `T08-S01:e6352`.
  Resolving the drawing's _presentation_ (P6-B2.4B) did not and does not
  retroactively clear its QA severity; that would require re-running QA,
  which this task does not do.
- **`blockingCount`/`warningCount` stay `1`/`2`** in the manifest — frozen
  historical import-time metrics, unaffected by this or any disposition
  change (same rule already established in P6-B2.2/P6-B2.4B).
- **Only `qaSha256` refreshed** in the manifest — the sole file that
  actually changed bytes. `mdxSha256`/`failureReportSha256`/
  `sourceSha256` and `status` (`in_review`) are unchanged.

## Files and migrations changed

- `docs/contracts/content.md` — new "P6-B2.5 (2026-08-16)" Amendments
  entry naming the third exception lesson, its retained blocking item,
  and its waiver reference.
- `scripts/validate-content/validate.py` — `P6_OWNER_APPROVED_PUBLISH_SLUGS`
  now includes `"bang-tuan-hoan"`; `PUBLISH_WAIVER_REQUIRED_ACKNOWLEDGED_BLOCKED_ITEMS`
  now includes `"bang-tuan-hoan": {"T02-S01:d1402"}`. No waiver-field
  validation logic (`validate_publish_waiver`) was touched — the existing
  checks apply unchanged to the new lesson.
- `scripts/validate-content/README.md` — short note that the P6.2-style
  exception now covers three lessons, naming `bang-tuan-hoan` and its
  acknowledged blocked item.
- `content/qa/pending/bang-tuan-hoan.json` — `approvedForPublish` `false`
  → `true`; new `publishWaiver` object (exact shape above).
  (`sha256:27653234105e0c255464c91153f2c041133b6d974513295d0ba9eb2d0d4d4738`)
- `content/pilot-staging-manifest.json` — Topic 2's `qaSha256` refreshed
  to match; `mdxSha256`/`failureReportSha256`/`sourceSha256`/`status`/
  `blockingCount`/`warningCount` and every other lesson's entry unchanged.
- `tests/content/topic2-owner-qa.test.ts` — inverted the
  `approvedForPublish`/no-waiver assertions to the opposite (now `true` +
  full waiver-shape assertion + `unresolvedBlockingCount: 1` +
  acknowledges `T02-S01:d1402` + exact `doesNotAuthorize` set); kept the
  assertion that `d1402` remains in `unresolved` at `severity: "blocking"`;
  kept the assertion that lesson `status` stays `in_review`, never
  `published`.
- `tests/content/operational-acceptance.test.ts` — added a durable
  synthetic-corpus test proving `bang-tuan-hoan` validates cleanly on the
  allowlist with a well-formed waiver, an invalid waiver on it still
  fails, and a non-allowlisted lesson still cannot set
  `approvedForPublish: true` at all (negative control, proving the
  allowlist gate itself still works).
- `tests/e2e/pilot-staging.spec.ts` — added an assertion that Topic 2's
  banner still reads `[BẢN ĐANG DUYỆT] ... chưa xuất bản` and never shows
  "đã xuất bản"/"published", unaffected by the new publish-exception
  waiver (no UI surfaces `approvedForPublish` directly; this proves that
  absence, not merely assumes it).
- `docs/handoffs/P6/P6-B2.5-topic2-approve-for-publication-claude.md` —
  this handoff.
- `docs/handoffs/P6/COORDINATION.md` — records this step, its scope, and
  that it is not a publication/production decision.
- Migrations: none. **Not changed:** `content/topics/chuyen-de-02/bang-tuan-hoan.mdx`,
  any file under `public/`, `content/qa/pending/bang-tuan-hoan.remediation-queue.json`,
  the failure report, any T06/T08/T24 content file, `package.json`,
  `package-lock.json`, any auth/R2/workflow file.

## Verification

- `npm run content:validate` — **PASS**, `{"valid": true, "errors": []}`.
- Full unit suite (`npx vitest run`) — **PASS**, 51 test files / 312
  tests (308 prior + 1 net new in `topic2-owner-qa.test.ts` + 3 new in
  `operational-acceptance.test.ts`).
- `npm run build` — **PASS**, same pre-existing Turbopack tracing warning
  as every prior P6 phase.
- Focused Playwright (`tests/e2e/pilot-staging.spec.ts`,
  `tests/e2e/p5-library-search.spec.ts`) — **PASS**, 27/27 (1 new
  P6-B2.5 test); Topic 2's banner confirmed to still read the
  `in_review`/`chưa xuất bản` text, never "published"/"đã xuất bản".
- `npm run verify` (format/lint/typecheck/tests/build) — **PASS**.
- `git diff --name-status origin/main` — confirmed **no**
  `content/topics/**` or `public/**` file changed.
- T06/T08/T24 content, QA, remediation-queue and failure-report files
  confirmed byte-identical to `origin/main`.

## Environment and external services

- No secret was read, printed, or changed.
- No R2, Neon, OAuth, production, public bucket, or deployment action
  occurred.

## Remaining work, known issues and risks

- **Not a publication decision.** `bang-tuan-hoan` remains `status:
"in_review"`; `published` is not set anywhere; no production
  deployment, public bucket access, or automatic publication is
  authorized by this or any prior P6 task. The staging banner still
  reads "chưa xuất bản" (not yet published).
- **`T02-S01:d1402` stays in the QA record as a retained, acknowledged
  blocking item.** This waiver does not resolve it, hide it, or change
  its severity — it only authorizes the lesson to carry
  `approvedForPublish: true` despite it, exactly like T08's own retained
  `T08-S01:e6352`.
- **P7 (production/public access) remains an entirely separate, later,
  explicit Owner decision.** This task does not narrow, imply, or
  schedule that decision.
- No further P6 work is authorized by this task. Any next topic import
  needs its own fresh Owner decision and preflight.

## Plan deviations

- None. This task implemented exactly the T06/T08-style exception the
  Owner authorized, using the exact same validator/contract mechanism
  already proven for those two lessons, and touched no file outside its
  stated narrow scope.

## Required inputs for the next task/phase

- `docs/handoffs/P6/P6-B2.5-topic2-approve-for-publication-claude.md` and
  the updated `docs/handoffs/P6/COORDINATION.md`.
- Integration review and Owner merge decision for this PR.
- After merge: Topic 2 sits alongside T06/T08 in governance state
  (`in_review`, `approvedForPublish: true`, retained/acknowledged
  blocking debt). No further P6 work is scoped; any next topic import or
  P7 production discovery needs a fresh, explicit Owner decision.
