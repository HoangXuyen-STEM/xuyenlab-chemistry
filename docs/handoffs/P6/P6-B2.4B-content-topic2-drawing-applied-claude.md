# Handoff P6-B2.4B (content) — Claude Code

## Status

Complete. Applies the P6-B2.4B policy (PR #47, merged) to the real
`T02-S01:d1402` item: `status: "applied"`, `remediationChoice:
"reviewed-image-fallback"`, the warning Callout removed, the
Owner-approved candidate SVG (PR #46) now the live replacement. This is
not a publication decision — `approvedForPublish` stays `false`
throughout.

## Ownership / authorization

- **Owner decisions already recorded, this task only implements them:**
  visual approval of the PR #46 candidate ("OK hình", 2026-08-15), and
  authorization of the P6-B2.4B policy path (merged as PR #47) over the
  "record now, Callout stays" alternative.
- Claude Code records the disposition per the merged policy's exact
  required fields — this is content recording, not a new chemistry or QA
  discretion decision.
- Integration review before merge; not merged by this task.

## Prerequisites re-verified before any edit

- PR #47 state: `MERGED`, merge commit
  `33ba8b0fe4968c0000d0fb7711cf074da3b2b181` (matches this task's stated
  baseline exactly).
- `scripts/validate-content/validate.py` confirmed to contain
  `validate_applied_image_fallback` and
  `REMEDIATION_APPLIED_IMAGE_FALLBACK_KINDS = {"drawing"}`;
  `docs/contracts/content.md` confirmed to contain the "Applied
  `reviewed-image-fallback`" section.
- The merged candidate `ChemFigure` matched the required exact strings
  byte-for-byte before any edit: `src`
  (`/staging-assets/lessons/51/518163475c46...svg`), `alt` (the full
  Vietnamese relationship description), `caption` ("Nguồn T02-S01, hình
  vẽ tái tạo (thay AutoShape Word T02-S01:d1402)"), `sourceId="T02-S01"`.
  The asset file's own `sha256sum` matched its filename.
- The drawing's warning Callout was still present in the MDX before this
  task's edit.
- `T02-S01:d1402` was still `status: "blocked"`, `remediationChoice:
"remain-blocking"` before this task's edit.
- `approvedForPublish: false`, no `publishWaiver`, confirmed before and
  after.

## Decisions and assumptions

- **`ownerDecision.altText`/`caption` set to the exact `ChemFigure`
  `alt`/`caption` strings**, verified programmatically byte-identical
  (not just visually) before committing — the policy's validator rule
  requires the pairing to match on the _same_ element exactly.
- **QA record (`content/qa/pending/bang-tuan-hoan.json`) left completely
  untouched.** `T02-S01:d1402` already sat in `unresolved` at `severity:
"blocking"` with its original description — this is the correct,
  final state for an `applied` item per the policy (mirrors T06's own
  four `applied`/`reviewed-latex-mdx` items, which likewise stay in
  `unresolved` unchanged). No byte of this file needed to change, so
  `qaSha256` in the manifest is unchanged too — refreshing a hash that
  didn't need to change would be a needless diff.
- **`blockingCount`/`warningCount` stay `1`/`2`** — per the policy's own
  contract text and this project's established rule, these are frozen
  historical import-time metrics, not a live recount. Verified this is
  still true after the edit (unchanged in the manifest, confirmed by the
  passing `content:validate` and the dedicated E2E assertion below).
- **Only `mdxSha256` refreshed** in the manifest — the sole file that
  actually changed bytes among the three sha-tracked fields.
- **Failure report untouched** (historical import evidence, per every
  prior P6 content task's own rule).
- **The issue ID stays traceable in the MDX** without the Callout: the
  candidate `ChemFigure`'s own `caption` already contains the literal
  string `T02-S01:d1402`, satisfying `validate.py`'s pre-existing "hidden
  unresolved blocking issue" rule (a blocking item's ID must appear
  somewhere in the MDX body) without needing any new text.

## Files and migrations changed

- `content/qa/pending/bang-tuan-hoan.remediation-queue.json` —
  `T02-S01:d1402`: `status` `blocked` → `applied`, `remediationChoice`
  `remain-blocking` → `reviewed-image-fallback`, `previewPath` set to the
  candidate SVG path, `ownerDecision` updated (`decidedAt: "2026-08-15"`,
  `altText`/`caption` populated exactly matching the `ChemFigure`,
  `reviewedLatex` stays `null`, `qaNote` records the Owner's PR #46
  approval and the P6-B2.4B policy basis). `T02-S01:i6022`/`T02-S01:t7931`
  entries byte-unchanged.
- `content/topics/chuyen-de-02/bang-tuan-hoan.mdx` — the drawing's warning
  `<Callout>` removed; the candidate `<ChemFigure>` (already merged in PR
  #46) is untouched; no other prose/table/image/frontmatter byte changed.
  (`sha256:d5a43992d9e767755aaae6735db07ca9522600a668b1c1604176dcbec355dcd1`)
- `content/pilot-staging-manifest.json` — Topic 2's `mdxSha256` refreshed
  to match; `qaSha256`/`blockingCount`/`warningCount`/`status`/provenance
  and every other lesson's entry unchanged.
- `src/app/fixtures/pilot/chuyen-de-02/bang-tuan-hoan/body.mdx` —
  regenerated from the canonical MDX minus frontmatter (mechanical sync,
  verified byte-identical by the pre-existing regression test).
- `tests/content/topic2-owner-qa.test.ts` — updated the drawing-specific
  assertions: `applied`/`reviewed-image-fallback` (not
  `blocked`/`remain-blocking`); `altText`/`caption` populated for the
  drawing but still `null` for the two accepted-with-limitation items;
  the "did not change ... Callout" test split into an unchanged-image
  assertion plus a new assertion that the Callout is gone and the
  candidate `ChemFigure` (byte-matching the PR #46 candidate) plus the
  traceable issue ID are present instead.
- `tests/e2e/pilot-staging.spec.ts` — replaced the now-obsolete "keeps
  Topic 2's blocking drawing Callout visible" test (P6-B2.2-era, assumed
  the Callout would always exist) with two P6-B2.4B tests: the Callout is
  gone and the candidate diagram is visible and traceable by issue ID;
  and the manifest's frozen blocking/warning counts plus the `in_review`
  banner are unaffected by the disposition change.
- `docs/handoffs/P6/P6-B2.4B-content-topic2-drawing-applied-claude.md` —
  this handoff.
- `docs/handoffs/P6/COORDINATION.md` — records PR #47 merged, this
  content step complete and awaiting merge, `T02-S01:d1402` now
  `applied`, still not a publication decision, next gate is Owner
  discretion.
- Migrations: none. **Not changed:** `content/qa/pending/bang-tuan-hoan.json`,
  `content/qa/import-reports/bang-tuan-hoan.failure.json`, any T06/T08/T24
  file, `scripts/validate-content/validate.py`, `scripts/import-docx/*`,
  `docs/contracts/*`, `package.json`, `package-lock.json` (all verified
  via `git diff --stat`/`--name-status` against `origin/main`).

## Verification

- `npm run content:validate` — **PASS** on the first attempt,
  `{"valid": true, "errors": []}` — this is the first time the P6-B2.4B
  validator path has actually been exercised by real committed content.
- Full unit suite (`npx vitest run`) — **PASS**, 51 test files / 308
  tests (307 prior + 1 net new in `topic2-owner-qa.test.ts`).
- `npm run build` — **PASS**, 15 static pages, same pre-existing
  Turbopack tracing warning as every prior P6 phase.
- Focused Playwright (`tests/e2e/pilot-staging.spec.ts`,
  `tests/e2e/p5-library-search.spec.ts`) — **PASS**, 26/26 (2 new
  P6-B2.4B tests, 1 replaced test, everything else unchanged and still
  green — including Topic 2's `acceptedLimitationsCount: 2`, unaffected
  since `applied` is not `accepted-with-limitation`).
- `npm run verify` (format/lint/typecheck/tests/build) — **PASS**.
- `npm run pdf:dry-run` — **PASS**; Topic 2's PDF regenerated under its
  new content hash (`d5a43992...pdf`, 138,720 bytes); T06/T08/T24 PDFs
  unchanged.
- Local production-build verification of the real rendered page
  (`npm start`, isolated port, killed after use): banner still `[BẢN
ĐANG DUYỆT]`; blocking-Callout locator count `0`; candidate diagram
  visible; page text still reads "1 mục chặn xuất bản · 2 cảnh báo".
  Screenshot confirms visually: no warning box below the table, the
  candidate diagram in its place, and the accepted-limitations section
  below still lists exactly the same two items (image, table) as before.
- `git diff --stat`/`--name-status origin/main` against T06/T08/T24
  content/QA/remediation-queue/failure-report paths, Topic 2's own QA
  record and failure report, `scripts/import-docx`,
  `scripts/validate-content`, `docs/contracts`, `package.json`,
  `package-lock.json` — empty/unchanged.

## Environment and external services

- No secret was read, printed, or changed.
- No R2, Neon, OAuth, production, public bucket, or deployment action
  occurred. The local `npm start` server above was bound to an isolated
  port and killed immediately after use.

## Remaining work, known issues and risks

- **Not a publication decision.** `T02-S01:d1402` being `applied` means
  the Owner-approved visual replacement is live; it does not change
  `approvedForPublish` (`false`), grant a `publishWaiver`, or promote any
  lifecycle field beyond what was already `in_review`.
- **No further P6 work is authorized by this task.** Any next topic
  import needs its own fresh Owner decision and preflight, per the
  standing P6 pattern; nothing here changes that.
- The SVG candidate's own visual/pedagogical correctness was already
  Owner-approved in PR #46; this task only records that approval under
  the newly authorized vocabulary and performs the mechanical MDX/QA
  bookkeeping — no new chemistry or design judgment was made here.

## Plan deviations

- None. This task implemented exactly the disposition the merged P6-B2.4B
  policy defines, using exactly the Owner-specified field values, and
  touched no file outside its stated narrow scope.

## Required inputs for the next task/phase

- `docs/handoffs/P6/P6-B2.4B-content-topic2-drawing-applied-claude.md`
  and the updated `docs/handoffs/P6/COORDINATION.md`.
- Integration review and Owner merge decision for this PR.
- After merge: the Topic 2 loop (P6-B2.0 → P6-B2.4B) is fully closed, with
  0 remaining structurally-blocking presentation gaps for staging/
  teacher-led use (the drawing is now applied, not merely accepted). Any
  further P6 work needs a fresh Owner decision.
