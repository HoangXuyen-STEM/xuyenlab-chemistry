# Task P4.2 — Claude Sonnet: MDX, responsive UI and print polish

## Assignment

Use P4.1's real Topic 6/8 `draft` output to make the staging lesson readable on mobile,
desktop and A4 print. Perform only mechanical MDX cleanup and presentation fixes.
Do not change chemical meaning, equations, answers or QA status; record uncertain
content as blocking for the project owner.

## Ownership

- Branch: `phase/p4-2-ui-print` from the accepted P4.1 baseline.
- Allowed: Topic 6/8 MDX presentation-only edits, `src/components/mdx/**`, P4 staging
  preview routes/styles, UI/print tests and `docs/handoffs/P4/P4.2-claude.md`.
- Forbidden: importer/validator logic, auth/account routes, DB/R2 runtime adapters,
  package/lockfile, production workflow, ADR/contracts, `published` status.

## Definition of done

- Topic 6/8 staging routes live under `/fixtures/pilot/**` and are accessible via
  Vercel preview without app login; do not put them under the private route group.
- Headings/TOC, tables, figures, math fallbacks, Example/Hint/Solution and warning
  callouts render without hiding converter failures.
- Mobile widths do not overflow except intentional scrollable tables/math.
- A4 print keeps captions/content, avoids important clipping and exposes Hint/Solution.
- Keyboard/focus/landmark/image-alt behavior is reviewed; unknown alt text remains a
  visible QA issue rather than invented copy.
- Run format/lint/typecheck/tests/build, write handoff, commit/push; do not merge.
