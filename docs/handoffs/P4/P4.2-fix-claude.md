# Handoff P4.2-fix — Claude Code (Sonnet)

## Status

Complete. Both verified PR #17 baseline defects fixed, with regression coverage now
committed and green.

## Inputs read

- `AGENTS.md`, `CLAUDE.md`, `KE_HOACH_XUYENLAB_CHEMISTRY.md`
- `docs/handoffs/P4/COORDINATION.md`, `docs/handoffs/P4/P4.2-claude.md`
  (original P4.2 handoff), `docs/handoffs/P4/P4.3-copilot-task.md`
- The blocked P4.3 handoff draft and its two uncommitted test files
  (`tests/e2e/pilot-staging.spec.ts`, `tests/content/pilot-content-regression.test.ts`),
  recovered from a local `git stash` on an unrelated branch — never previously
  pushed to any branch or PR. These are what the task packet's "E2E regression"
  refers to.
- `src/components/pilot-reader/PilotLessonShell.tsx`, `src/components/mdx/Callout/Callout.tsx`
- `content/topics/chuyen-de-06/dong-hoa-hoc.mdx`,
  `content/topics/chuyen-de-08/dung-dich-va-can-bang-hoa-hoc.mdx`,
  `content/pilot-staging-manifest.json`,
  `content/qa/import-reports/dong-hoa-hoc.failure.json`,
  `content/qa/pending/dong-hoa-hoc.json`

## Completed

- **Defect 1 (accessibility landmark).** `PilotLessonShell.tsx` wrapped the
  entire lesson (banner, breadcrumb, header, TOC, article) in a plain `<div>`;
  no `<main>` landmark existed anywhere on either pilot lesson route. Changed
  the outermost wrapper from `<div className={styles.page}>` to
  `<main className={styles.page}>`, matching the existing `/fixtures/mdx`
  precedent. Verified exactly one `<main>` per route both in `next dev`
  (`getByRole("main")` count = 1) and in the production build output
  (`grep -c '<main' .next/server/app/fixtures/pilot/**/*.html` = 1 for both
  routes). The E2E assertion (`page.getByRole("main")).toHaveCount(1)`) was
  **not weakened** — still asserts the ARIA `main` role, not `article`.
- **Defect 2 (Topic 6 "95 vs 96" mismatch) — root-caused, no content changed.**
  All 96 `T06-S01:*` blocking IDs render, are visible (`offsetParent !== null`),
  and `content/topics/chuyen-de-06/dong-hoa-hoc.mdx` is still byte-identical to
  `body.mdx` (`diff` empty) and matches its manifest `mdxSha256`. Nothing is
  missing, replaced, or hidden. The exact missing-from-count issue ID is
  **`T06-S01:d6703`** (a `kind: "drawing"`, `severity: "blocking"` fallback,
  confirmed present and `outcome: "fallback"` in both
  `content/qa/import-reports/dong-hoa-hoc.failure.json` and
  `content/qa/pending/dong-hoa-hoc.json`). Its `Callout` renders correctly but
  with `title="Hình vẽ Word cần biên tập"` instead of the generic
  `title="Đối tượng Word cần biên tập"` used by the other 95 — this is P4.1's
  own deliberate taxonomy (fallback title keyed by `kind`), not a bug:
  Chuyên đề 8's canonical MDX independently confirms the exact same
  three-way split — `embeddedObject` → "Đối tượng Word cần biên tập" (121),
  `drawing` → "Hình vẽ Word cần biên tập" (3), `formula` → "Công thức cần
  biên tập" (2) — summing to its manifest's 126 exactly. The stashed P4.3
  E2E spec's `renders all blocking fallback warnings visibly with stable
  issue IDs` test hardcoded only the first title string as its blocking-Callout
  selector, so it undercounted by 1 on Topic 6 and would have undercounted by
  5 on Topic 8 too (masked because the test's `for` loop throws on Topic 6
  first and never reaches Topic 8).
  - **Fix (in the test, confirmed with the user before applying):** broadened
    `tests/e2e/pilot-staging.spec.ts`'s blocking-Callout locator to match all
    three legitimate `aria-label` variants instead of one. The assertion
    itself was **not weakened**: still asserts `toHaveCount(lesson.blockingCount)`
    (96 / 126, unchanged, sourced from the manifest) and the same
    `T0[68]-S01:[a-z0-9]+` stable-ID + `offsetParent !== null` visibility
    check on every matched element.
  - No `content/**`, `content/pilot-staging-manifest.json`, or QA/failure
    report file was touched — the "traceability mechanism" updated was the
    test's own locator, not any content or QA record.

## Decisions and assumptions

- The two P4.3 test files existed only as uncommitted, unpushed local state
  (a `git stash` entry on `phase/p4-5-latex-7-samples`, untouched by any
  branch or PR) — recovered read-only via `git show <stash-untracked-tree>:<path>`
  and copied in, not modified from their original P4.3 authorship except the
  one locator fix above and three pre-existing `tsc --noEmit` type errors
  (`offsetParent`/`alt` not present on the `SVGElement | HTMLElement` union
  Playwright infers for generic element handles) fixed with narrow
  `as HTMLElement` / `as HTMLImageElement` casts so `npm run typecheck` is
  clean — no assertion logic changed by these casts.
  - Asked the user explicitly before touching `tests/e2e/pilot-staging.spec.ts`
    (normally Copilot/P4.3 territory per `AGENTS.md`) since editing it was
    the more defensible fix versus mutating canonical content to fit an
    overly narrow selector; the user confirmed committing the fix into PR #17.
- Did not change `playwright.config.ts` — the stashed P4.3 draft had an
  unrelated port/`PLAYWRIGHT_BASE_URL` convenience change; the existing
  config (port 3100, auto-started `webServer`) already runs both new files
  correctly, so it was left untouched to minimize footprint.
- Did not touch `content/topics/chuyen-de-06/dong-hoa-hoc.mdx`,
  `content/pilot-staging-manifest.json`, or any QA/failure-report file:
  relabeling `T06-S01:d6703` to the generic title would have misclassified a
  real `drawing`-kind fallback as a generic `embeddedObject` one and broken
  consistency with Chuyên đề 8's identical, already-shipped convention — a
  content/QA meaning change, out of this task's scope.

## Files and migrations changed

No migrations, no `package.json`/lockfile, no auth/DB/R2, no `content/**`,
no manifest, no QA/failure-report file, no ADR/contract change.

- `src/components/pilot-reader/PilotLessonShell.tsx` — outer wrapper
  `<div>` → `<main>` (defect 1).
- `tests/e2e/pilot-staging.spec.ts` — new (recovered from stash + fixed):
  blocking-Callout locator broadened to all three legitimate `aria-label`
  title variants (defect 2); three `tsc` casts unrelated to the fix.
- `tests/content/pilot-content-regression.test.ts` — new (recovered from
  stash, unmodified): manifest/failure-report/QA parity regression suite.
- `docs/handoffs/P4/P4.2-fix-claude.md` — this handoff.

## Verification

| Command | Result |
| --- | --- |
| `npm run format:check` | pass (after `prettier --write` on the 2 new test files) |
| `npm run lint` (`--max-warnings=0`) | pass |
| `npm run typecheck` | pass, 0 errors |
| `npm run test` | **106/106 pass** (100 baseline + 6 new content-regression) |
| `npm run build` (Turbopack) | pass; both pilot routes still prerendered static (`○`) |
| `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate-content/validate.py --json` | `{"valid": true, "errors": []}` |
| `npx playwright test tests/e2e/pilot-staging.spec.ts` | **7/7 pass** (previously 2 failing: landmark + blocking count) |

Direct evidence for "all 96 expected blocking IDs are traceable and visible" on
Topic 6 (commands run against `content/topics/chuyen-de-06/dong-hoa-hoc.mdx`
and the built HTML):

- `diff <(tail -n +17 content/topics/chuyen-de-06/dong-hoa-hoc.mdx) src/app/fixtures/pilot/chuyen-de-06/dong-hoa-hoc/body.mdx` — empty (still byte-identical).
- `sha256sum content/topics/chuyen-de-06/dong-hoa-hoc.mdx` == manifest's
  recorded `mdxSha256` for `dong-hoa-hoc` — matches, unchanged.
- `grep -oE 'T06-S01:[a-z0-9]+' content/topics/chuyen-de-06/dong-hoa-hoc.mdx | sort -u | wc -l` → 96.
- Rendered HTML (`next dev`, `/fixtures/pilot/chuyen-de-06/dong-hoa-hoc`):
  `<aside>` count = 96 total (95 `aria-label="Đối tượng Word cần biên tập"` +
  1 `aria-label="Hình vẽ Word cần biên tập"`); unique `T06-S01:*` IDs in the
  page = 96; `T06-S01:d6703` present.
- Production build output
  (`.next/server/app/fixtures/pilot/chuyen-de-06/dong-hoa-hoc.html`):
  `<main>` count = 1.
- `content/qa/import-reports/dong-hoa-hoc.failure.json`: 96 blocks with
  `severity: "blocking"`, all `outcome: "fallback"`, all present in
  `content/qa/pending/dong-hoa-hoc.json`'s `unresolved` list (pre-existing;
  reconfirmed, not modified).

## Environment and external services

None read or required. No environment variable, secret, database, auth
provider, or R2 bucket was touched.

## Remaining work, known issues and risks

- **P2 / P4.3 owner:** the two test files (`tests/e2e/pilot-staging.spec.ts`,
  `tests/content/pilot-content-regression.test.ts`) are now committed on
  `phase/p4-2-ui-print` (PR #17) rather than on their originally-assigned
  `phase/p4-3-content-e2e` branch, since they were the only artifact that
  could prove defect 2 was fixed without touching content/QA files. P4.3
  should treat these as its committed starting point (rebase/cherry-pick from
  PR #17 once merged) rather than re-authoring them.
- **P2 / observed:** `playwright.config.ts` still hardcodes port 3100 and
  always spawns its own `next dev`; the stashed P4.3 draft's
  `PLAYWRIGHT_BASE_URL` override (to run against an already-running server)
  was not adopted here to keep this fix minimal — worth revisiting if P4.3's
  CI needs it.
- All other P4.2 remaining-work items (chemical-correctness QA, `body.mdx`
  staleness-on-reimport, etc.) are unchanged from the original
  `docs/handoffs/P4/P4.2-claude.md` and still apply.

## Plan deviations

- Committed two test files (`tests/e2e/pilot-staging.spec.ts`,
  `tests/content/pilot-content-regression.test.ts`) that are outside this
  task's UI/print default ownership and normally belong to P4.3/Copilot per
  `AGENTS.md`. Flagged here as a deviation, not silently done: the task
  packet explicitly discussed this exact E2E test's assertions ("do not
  weaken... from main to article", "do not change test expectation"), which
  only makes sense if editing it was in scope, and the user was asked and
  explicitly approved committing the fix into PR #17 before it was done.
- No chemistry, content, manifest, or QA-record change was made.

## Required inputs for the next task/phase

- PR #17 (`phase/p4-2-ui-print` → `main`) is pushed with both fixes and is
  green end-to-end; still not merged, per instruction.
- P4.3 (Copilot): `phase/p4-3-content-e2e` should be rebased on the updated
  `phase/p4-2-ui-print` tip and treat the two test files already there as its
  starting point rather than recreating them.
- Anyone touching Callout-based blocking fallbacks: the three-way title
  taxonomy (`Đối tượng Word cần biên tập` / `Hình vẽ Word cần biên tập` /
  `Công thức cần biên tập`, keyed by the failure report's `kind` field) is a
  real, load-bearing P4.1 convention now — any future selector or count
  assertion over blocking Callouts must account for all three, not just the
  first.
